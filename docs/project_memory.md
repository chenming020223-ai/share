# 项目记忆

日期：2026-05-27  
用途：记录本项目长期上下文，避免后续迭代丢失关键决策。

## 1. 项目身份

- 项目名：世界杯预测。
- 项目目标：输入两支球队，自动抓取数据并预测 90 分钟赛果。
- 使用方式：只在甲方电脑本地运行。
- 主要界面：本地 Web 页面，地址默认为 `http://127.0.0.1:8765`。
- 命令行保留为调试和批处理入口。

## 2. 甲方已确认决策

| 决策项 | 已确认内容 |
|---|---|
| 数据源 | API-Football |
| 模拟舱规则 | 研究方向须通过盘口校验、优势门槛和保守研究试算 EV；正式信号须另经 `pfinal` 验证 |
| 预测口径 | 只看 90 分钟赛果 |
| 部署方式 | 本地电脑运行 |
| 项目名称 | 世界杯预测 |
| 品牌主色 | 橘色 |
| Logo | 项目内自设计 |
| 报告导出 | 需要中文 Excel / PDF 报告导出 |
| 展示标准 | 比赛需显示具体联赛名称，开赛时间统一北京时间，联赛和球队对用户统一显示官方中文名称 |
| 模型治理 | API 模式在 `pfinal` 经校准回测验证前仅展示研究试算 EV，不启用正式模拟信号；重大模型分歧时暂停公开 EV 数值 |

## 3. 当前已实现功能

### 产品功能

- 本地网页界面。
- 网页已改为真实赛前比赛单入口，使用 API-Football，初次打开不会自动生成演示运行记录。
- 展示融合概率（非 `pfinal`）、`pbase` 基础概率、`qmkt` 市场去水概率展示。
- 预期进球展示。
- 最可能比分展示。
- 胜平负、大小球、让球模拟舱方向。
- 模型概率身份展示：`pbase` 基础概率、`qmkt` 市场去水概率、展示融合概率（非 `pfinal`）。
- 1000 元启动资金和均注金额输入。
- 盘口有效、研究试算 EV 达标、优势达标且保守研究试算 EV 仍为正时才保留研究方向。
- 2026-05-26 起，真实 API 模式在 `pshr/pfinal` 未验证前，将通过研究试算门槛的方向统一降级为待校准复核，模拟资金占用为零。
- 2026-05-27 起，当完整 Pinnacle 胜平负盘口中 `pbase` 与 `qmkt` 的最大偏差超过 15 个百分点时，整场标记为模型分歧异常，胜平负、大小球、让球 EV 均暂停公开展示；原始试算只保留在中文报告审计附录。
- 中文 Excel 报告导出。
- 中文 PDF 报告导出。
- 2026-05-27 起，网页采用石墨灰玻璃工作台布局，使用橘色信号 Logo，并以“预测工作台 / 数据处理 / 模型审计 / 历史报告”分区承载决策与复核。
- 2026-05-27 起，新增受控公网分享部署基线：`render.yaml` 提供 HTTPS 公网部署入口、持久存储和健康检查配置；`WORLDCUP_PUBLIC_MODE` 下 API 密钥仅由服务器托管，配合共享访问口令供小范围评审使用。当前状态为可发布，尚待甲方提供托管账号通道、新 API 密钥及分享口令后生成正式公网链接。
- 页面“数据处理”展示双方近 5–10 场有效赛果明细、场均进失球与积分等特征、累计积分线性图；只有单次盘口快照时不绘制虚假赔率走势。
- 2026-05-28 起，单场默认采用深度模式：在基础赛果、赔率、赛季统计和交锋外，补抓双方近期比赛的 xG、射门、射正、控球、红牌和点球事件，进入数据处理、快照和中文报告。
- 2026-05-28 起，新增每日批量建库入口 `worldcup_predictor.batch_collect`、`daily_batch_collect.command` 和 `install_daily_batch.command`；默认按北京时间今日甲级联赛、批量建库模式采集赛前快照，支持 macOS 每天 08:30 自动运行。
- 本地 SQLite 保存预测记录。
- 健康检查接口。
- 最近预测记录接口。
- 比赛展示字段中文化：
  - 球队名称输出 `homeZh`、`awayZh`。
  - 联赛名称输出 `leagueNameZh`。
  - 开赛时间输出 `kickoffBeijing`。
  - 中文报告、网页标题、赛程搜索结果、模拟舱方向优先使用受控中文展示名；未核定名称直接保留 API 英文原名，避免分析对象无法识别，并可继续补入名录。
- 数据质量评分：
  - 后端输出 `dataQuality`。
  - 三类市场分别判断：胜平负、大小球、让球。
  - 真实 API 模式下，如果质量低于候选门槛，方向为观望。
  - 缺少可靠球队强度数据时，会强制降级为 WATCH。
  - 大小球和让球只接受同公司、同盘口线成对赔率。
  - 两项盘口隐含概率和低于 98% 或高于 125% 时会被排除。
  - 网页和中文报告展示数据质量评分、质量等级、市场可用状态。
- 风险与回测基础：
  - API 模式保存原始数据快照，包括 fixture、odds、team statistics、h2h。
  - SQLite 包含 `api_snapshots`、`market_quotes` 和 `match_results`；`market_quotes` 规范化保存 Pinnacle 全场盘口、方向、赔率、去水概率和时点。
  - 新增 `worldcup_predictor/backtest.py`，可自动同步合格赛前预测的赛后 90 分钟结果并结算 ROI、最大回撤、Brier Score、Log Loss。
  - 新增 `worldcup_predictor/calibration.py`，按开赛时间划分开发/校准/验证区间，审计胜平负 `pbase -> pshr`。
  - 页面新增“校准与回测准入”状态区和“同步赛果”操作。
  - 今日随机比赛只抽取赛前比赛。
  - 页面新增“今日甲级联赛”入口，按北京时间只列出尚未开赛、赛事名称映射为足球甲级联赛的赛程，再由用户选择预测。
  - 人工输入比赛 ID 也必须为尚未开赛比赛，赛后快照不可进入验收样本。
- 交付稳定性：
  - API-Football 请求支持自动重试，默认 `API_FOOTBALL_RETRIES=3`。
  - SSL EOF、证书、限流、密钥错误会被结构化为中文错误。
  - 新增 API 连接检查接口 `/api/api-status`。
  - 新增双击启动脚本 `start_worldcup_predictor.command`。
  - 新增首次安装脚本 `setup_worldcup_predictor.command`。
  - 新增甲方交付说明 `交付说明.md`。

### 技术模块

- `worldcup_predictor/api_football.py`：API-Football 客户端。
- `worldcup_predictor/auto_predict.py`：自动预测流程。
- `worldcup_predictor/betting.py`：模拟舱和 EV 计算。
- `worldcup_predictor/model_governance.py`：概率版本身份、正式 EV 准入状态和未校准信号闸门。
- `worldcup_predictor/backtest.py`：赛后结算和最小回测指标。
- `worldcup_predictor/calibration.py`：时间切分校准审计、`pshr` 候选拟合与 `pfinal` 准入状态。
- `worldcup_predictor/data_quality.py`：数据质量评分、市场完整性判断、低质量研究方向降级。
- `worldcup_predictor/market.py`：赔率解析。
- `worldcup_predictor/report.py`：中文报告生成。
- `worldcup_predictor/storage.py`：SQLite 留档。
- `worldcup_predictor/web_server.py`：本地 Web 服务。
- `worldcup_predictor/localization.py`：球队/联赛中文名称和北京时间转换。
- `web/`：前端页面、样式、交互、Logo。

### 最新研究文档

- `docs/model_optimization_research.md`：模型优化与增强理论研究，提出五层增强架构：
  - 球队评分层。
  - 进球分布层。
  - 市场校准层。
  - 世界杯情景层。
  - 风险与回测层。
- `docs/model_formulas.md`：模型完整计算公式，包含当前 MVP 已实现公式和 V2 增强公式。

## 4. 当前验证状态

- 内部回归样本预测可用，但不在甲方网页入口展示。
- 网页可在甲方电脑打开。
- SSL 证书问题已通过 `certifi` 优先证书链修复。
- 单元测试通过：2026-06-04 最近一次为 `73 tests OK`。
- 石墨灰玻璃工作台已完成桌面与 `390px` 窄屏浏览器验收，无横向溢出。
- 现有本地库已保存 `149` 条预测、`101` 条 API 快照、`1451` 条结构化报价、`20` 场赛果和 `6` 个批量批次。
- 当前合格校准样本为 `15/100`，独立比赛 `15/100`，校准区间 `3/20`，验证区间 `3/20`，正式 EV 保持关闭。
- API-Football 已能连通到业务层。
- 2026-05-26 按北京时间抓取到 `10` 场尚未开赛的甲级联赛；页面实际预测运行 `56` 选择“哈萨克斯坦足球甲级联赛：阿斯塔纳二队 vs 汗腾格里”（22:00 北京时间）。当时未取得优先级内可用盘口，三类市场均为市场缺失、占用资金 `0` 元，因此快照不进入合格校准样本。
- 中文展示基于项目受控映射名录，API 英文名称和 ID 始终保留用于审计；API-Football 未提供中文官方名字段，新增译名应在甲方交付验收时核定。
- 2026-05-27 页面真实验收运行 `67`：波黑塞族共和国足球甲级联赛“奥马尔斯卡 vs 弗拉塞尼察”（23:00 北京时间）；双方各取得 `10/10` 场近期有效样本，数据处理页展示特征表与三张曲线。本场无指定庄家全场盘口，模拟舱正确保持占用 `0` 元；中文 Excel/PDF 已导出。
- 已观察到的问题：输入 `Mexico` 和 `USA` 时，API-Football 没有返回未来直接交锋，因此提示找不到 upcoming head-to-head fixture。

## 5. 当前主要问题

### P0：API 模式依赖未来直接交锋或 fixture_id

当前已新增缓解方案：

- 后端新增 `/api/search-fixtures`，用于按球队搜索未来赛程。
- 后端新增 `/api/random-today-predict`，用于从当天 API-Football 赛程随机抽一场并预测。
- 后端新增 `/api/today-fixtures?scope=first_division`，用于按北京时间抓取赛前甲级联赛候选。
- 前端新增“搜索比赛”“今日甲级联赛”和“今日随机比赛”按钮。
- 若没有未来直接交锋，会给中文提示。

仍需实机验证：

- 用户本机服务重启后，在页面点击按钮验证 API 返回结果。
- 今日随机比赛依赖当天 API-Football 是否存在 fixtures。

原始问题逻辑是：

```text
输入两队
  ↓
搜索球队 ID
  ↓
查未来 head-to-head fixture
```

如果两队没有已排定的未来交锋，API 会返回空。此时程序必须让用户填写 `fixture_id`，或者提供比赛搜索/选择功能。

影响：

- 普通用户不知道 fixture_id。
- 页面看起来像“预测失败”，实际是“没有可预测的已排定比赛”。

### P0：API 报错时右侧仍保留上一次结果

已改进：

- API 预测失败时，前端会把右侧改成“预测失败”状态。
- 导出按钮会禁用。
- 数据提示区会显示中文错误说明。

剩余风险：

- 需要用户重启本地服务后才能加载新后端接口。

### P1：真实赔率覆盖不稳定

API-Football 的赔率、大小球、让球覆盖取决于赛事、时间和 bookmaker。某些比赛可能没有完整市场。

已完成：

- 明确显示哪些市场可用。
- 对缺失赔率给中文解释。
- 缺失市场返回 `NO_MARKET`。
- 数据质量不足时，API 模式研究方向自动降级为观望。
- 指定庄家报价按市场键、盘口线、方向和赔率时点结构化存储。
- 已对存量原始赔率快照进行规范化回填。

后续需要：

- 积累足量赛前快照并按时点区分早盘、即时盘、临场盘。
- 增加收盘线对比。

### P1：模型仍是 MVP

当前模型适合演示和框架验证，但真实预测能力不足。尚未接入：

- World Football Elo。
- FIFA 官方排名。
- 伤停。
- 首发。
- 小组积分和出线形势。
- 天气。
- xG。
- 球员身价和阵容深度。

最新研究结论：

- 下一阶段不应先堆复杂模型，而应先补数据质量评分和市场完整性展示。
- 研究方向判断已包含 `data_quality_score` 门槛；正式信号仍须等待 `pfinal` 验证。
- 模型 V2 建议采用 `Rating + Goals + Market Calibration` 架构。
- 世界杯小组赛需要从“必须赢”字段升级为结果收益函数和出线压力模型。
- 当前展示融合概率不是正式 `pfinal`；在校准回测完成前，API 模式不启用正式模拟信号。

### P1：历史回测仍需扩展

当前已新增赛果同步和胜平负时间切分校准审计，但还不是完成验收的历史回测系统。现在展示的研究试算 EV 仍是模型估算差异，不是已经用大量历史比赛验证的长期优势；API 模式不将其作为正式信号。

需要：

- 继续积累或批量导入合格赛前预测和赛果，至少达到 100 场独立比赛。
- 为大小球和让球建立比分分布层校准验收。
- 输出长期资金曲线和分市场表现。
- 做 CLV、Brier Score、Log Loss、ROI 的阶段性报告。

## 6. 产品边界

- 不连接真实投注平台。
- 不保证收益。
- 模拟舱仅用于纸上研究和回测。
- 国家关系、商业收益等情景变量只低权重处理。
- 正式输出坚持概率表达，不使用“稳赢”等绝对措辞。

## 7. 最近一次用户反馈

用户截图显示：

- 本地网页已成功打开。
- 页面品牌和橘色主题已生效。
- API 模式能进入业务逻辑。
- 当前错误为：没有找到 `Mexico vs USA` 的未来直接交锋。

因此下一步重点已经从单点功能转向可交付稳定性：

1. 确保甲方可以双击启动本地网页。
2. API 网络失败时给出中文原因和恢复建议。
3. 用真实 API-Football 随机比赛做端到端验证。
4. 根据 API-Football 实际返回字段继续补市场完整性展示。

## 8. 模型增强下一步

已完成理论研究：`docs/model_optimization_research.md`。
已完成公式文档：`docs/model_formulas.md`。

建议执行顺序：

1. 增加数据质量评分模块。
2. 增加市场完整性展示。
3. 加入 Elo/FIFA 评分底座。
4. 重构进球模型，为低比分修正预留接口。
5. 建立历史回测和模拟舱结算闭环。

当前状态：

- 第 1、2 项已完成第一版。
- 已新增模型吸收规范和正式 EV 未校准闸门。
- 已完成市场键/赔率时点留档、赛果同步按钮和胜平负 `pshr` 时间切分审计第一版。
- 下一步应累计至少 100 场合格赛前已结算样本，加入收盘线/CLV，并为大小球和让球建设专项校准。

## 9. 2026-06-02 更新：抓取层与长期使用结构

本轮按用户要求吸收桌面《Football/精准程序》的抓取层思路，但不直接照搬模型。已落地：

- 新增庄家优先级：默认 `Pinnacle > Bet365 > Betfair > SBO > 10Bet > 1xBet`，首选缺失时按优先级回退。
- 每个市场独立记录实际使用庄家，例如胜平负、大小球、让球可分别保存来源；同一个盘口线仍禁止跨公司拼接。
- 新增缓存型 API-Football 客户端，后台批量建库可深度抓取近期赛果、技术统计和事件，同时复用缓存降低重复请求。
- 批量建库流程开始记录逻辑请求、实际 HTTP 请求、缓存命中和缓存未命中。
- 新增动态资金策略：默认启动资金 1000 分 5 份，本金以下按当前资金五等分，本金以上只将利润 50% 纳入重新分配。
- 新增模拟资金账本表，未来只有真正占用资金的 `BUY/PAPER_BUY` 才写入账本；正式 EV 未开放时不伪造下注记录。

仍需继续：

- 把批量建库入口做成网页多场分析页面。
- 增加模拟舱资金曲线、回撤曲线、信号原因统计和市场覆盖图。
- 对不同庄家的样本做分层校准，避免把 `Pinnacle` 与其他公司盘口混成一个未校验市场基准。
- 正式 EV 仍需通过 `pfinal` 时间切分、CLV、ROI 和大小球/让球专项验收后才能开放。

## 10. 2026-06-02 更新：批量工作台与历史台账

根据用户反馈，已完成三项可用性修正：

- 批量分析支持指定多个 API-Football fixture_id；输入框接受逗号、空格和换行分隔，搜索赛程结果也可一键加入批量池。
- 批量指定 ID 时，单个无效 ID 只进入失败列表，不中断其他比赛分析。
- 数据处理与历史报告中的图表改为模拟舱资金曲线，展示资金与期望资金走势。
- 历史报告列表升级为台账卡片，直接显示最高概率预测、模拟舱推荐/观望摘要、资金变化、数据质量和实际庄家，Excel/PDF 作为明细复核入口。

仍需继续：

- 为批量结果增加“批量总览页”，按联赛、推荐动作、市场覆盖和数据质量分组。
- 在模拟舱账本开始记录实际结算后，增加真实资金曲线、回撤曲线和分市场收益统计。

## 11. 2026-06-02 待登记问题：批量分析仍有漏洞

用户反馈：批量分析仍存在漏洞，当前先不继续更新程序，只登记为后续重点复核项。

初步归档为：

- 当前批量分析只是把多场比赛逐个调用单场预测，尚未形成完整的多场工作流。
- 批量结果缺少总览层，不能快速比较每场的推荐方向、数据质量、庄家覆盖、EV 状态和模拟资金占用。
- 批量资金逻辑需要复核：多场同批分析时，是否应按同一初始资金独立评估，还是按模拟舱顺序动态占用资金。
- 指定 ID 批量虽然支持输入，但仍需进一步检查异常 ID、重复 ID、已开赛比赛、无盘口比赛和跨日期比赛的提示是否足够清晰。

后续处理原则：

- 不直接扩大批量功能，先明确批量分析的业务定义：筛选器、排序逻辑、资金占用方式和输出总览。
- 批量分析应升级为“赛事池 + 批量总览 + 单场钻取 + 资金组合”的结构，而不是仅显示多条运行结果。

## 12. 2026-06-02 执行记录：批量赛事池第一版

已完成：

- 批量结果从简单运行列表升级为赛事池总览，返回成功场次、信号数、观望数、失败数、预计资金等汇总字段。
- 每场批量结果返回预测方向、预测概率、模拟舱方向、推荐原因、赔率、模型概率、市场概率、研究 EV、保守 EV、数据质量、盘口覆盖、实际庄家和资金占用。
- 批量失败增加分类标签：比赛 ID/赛程无效、非赛前比赛、盘口缺失、样本不足、API/网络异常、分析失败。
- 批量结果按推荐动作、数据质量、保守 EV、研究 EV 和预测概率排序，优先把更值得复核的比赛放在前面。
- 网页批量结果新增“查看单场”按钮，可从批量列表回到完整单场分析。
- 新增 `/api/prediction?run_id=` 读取已保存单场预测，服务于批量钻取和后续历史台账。

仍未完成：

- 批量资金仍是逐场独立评估，没有按同一批次顺序扣减资金。
- 还没有批量筛选器，例如联赛、国家、时间、数据质量、庄家覆盖、只看可推荐信号。
- 还没有真正的组合风险控制，例如同联赛集中度、同市场集中度、单批资金上限和最大回撤约束。
- 批量总览还未独立成完整页面，目前先显示在赛事选择区域。

## 13. 2026-06-02 执行记录：批量筛选与组合预案

已完成：

- 批量赛事池增加前端筛选：全部、只看信号、组合预案、只看观望、市场缺失、失败记录。
- 批量赛事池增加质量筛选：全部质量、高质量、可复核。
- 批量赛事池增加排序：推荐优先、质量优先、EV 优先、时间优先。
- 批量汇总新增研究组合预案：按推荐动作、数据质量、保守 EV 排序，单批建议占用不超过启动资金 50%。
- 组合预案返回候选数、纳入数、计划占用、剩余上限、期望资金、纳入 run_id、联赛暴露、市场暴露和集中度提示。
- 前端组合预案可以高亮被纳入的比赛，并展示“暂无候选/超资金上限/联赛集中/市场集中”等提示。

仍未完成：

- 批量资金尚未按真实顺序动态扣减；目前仍是研究预案。
- 批量结果仍位于左侧赛事选择区域，后续应独立成“批量赛事池”主页面。
- 筛选器还没有日期、国家、联赛、庄家、盘口覆盖和只看世界杯/甲级联赛等更细颗粒选项。
- 组合策略还缺少同队相关性、同开赛时间、赔率波动和最大回撤约束。

## 14. 2026-06-03 执行记录：批量赛事池独立主页面

已完成：

- 顶部导航新增“批量赛事池”页签，批量分析完成后自动切换到该主页面。
- 批量赛事池总览、筛选器、组合预案和单场钻取入口从左侧赛事选择栏迁移到主内容区。
- 左侧赛事选择栏只保留批量完成摘要，不再承载大列表，避免页面拥挤和阅读困难。
- 批量赛事池主页面使用更宽的网格展示汇总、资金预案和每场比赛字段。
- 保留“查看单场”入口，可从批量赛事池返回完整单场分析。

仍未完成：

- 批量赛事池已具备批次历史搜索和分页，但当前批量结果内部还没有按联赛/国家/开赛时间过滤。
- 批量赛事池结果需要批次 ID 持久化；该项已在下一阶段完成。
- 组合资金仍是研究预案，尚未进入真实模拟舱顺序扣减账本。

## 15. 2026-06-03 执行记录：批次 ID 与批量历史保存

已完成：

- 新增本地 `batch_runs` 表，保存每次批量分析的完整批量 JSON、日期、范围、指定 fixture_id、成功/失败场次、信号数、计划占用和期望收益。
- 批量分析完成后自动生成 `batchRunId`，并随接口返回给前端。
- 新增 `/api/recent-batches`，用于读取最近批量批次列表。
- 新增 `/api/batch-run?batch_id=`，用于按批次 ID 恢复完整批量赛事池。
- “批量赛事池”页面在没有当前批量结果时显示最近批次列表，点击“恢复批次”即可重建筛选器、组合预案和单场钻取入口。
- 恢复批次只读取本地 SQLite，不重新调用 API-Football，避免重复消耗请求数。

仍未完成：

- 批次列表的搜索、分页、重命名和备注已在下一阶段完成；删除功能暂不开放，避免误删历史样本。
- 批次之间还没有对比功能，例如不同日期/联赛批次的信号数、质量、收益预案对比。
- 批量模拟资金仍未进入按时间顺序动态扣减账本。

## 16. 2026-06-03 执行记录：批次管理第一版

已完成：

- `batch_runs` 增加 `title` 和 `notes` 字段，既保存列表摘要，也同步写回完整批次 JSON 快照。
- 新增 `/api/update-batch`，用于保存批次名称和备注；该接口只写本地数据库，不调用 API-Football。
- `/api/recent-batches` 默认返回最近 50 个批次，并优先使用用户命名作为批次标题。
- “批量赛事池”历史列表支持按名称、备注、日期和 fixture_id 搜索。
- 批次历史按每页 5 个展示，降低历史记录增多后的阅读压力。
- 每条历史批次直接展示成功/失败、信号数、组合占用、期望收益、指定 ID 和备注，不需要下载报告才能看懂。
- 批量结果页新增“查看历史批次”，无需刷新即可从当前批次切回历史档案。

仍未完成：

- 批量结果内部已支持联赛、开赛时段、庄家、盘口覆盖和关键词过滤；尚未支持国家过滤。
- 批次之间还没有对比看板，例如不同日期/联赛批次的信号率、质量均值、收益预案和失败原因分布。
- 删除/归档批次暂未开放；正式开放前应先设计误删保护。
- 批量模拟资金仍未按开赛时间顺序动态扣减并沉淀为完整账本。

## 17. 2026-06-03 执行记录：批量赛事池细筛增强

已完成：

- 批量赛事池内部新增关键词筛选，可按球队、比赛 ID、联赛、推荐方向、推荐原因和庄家搜索。
- 关键词筛选支持常用联赛缩写，例如英超、意甲、法乙等，可匹配项目展示的官方中文联赛名。
- 新增联赛筛选，可只看当前批次中的某一个联赛。
- 新增庄家筛选，并把单场显示从单一庄家升级为分市场来源，例如“胜平负=Pinnacle · 大小球=Bet365 · 让球=Bet365”。
- 新增盘口覆盖筛选：三项完整、部分可用、盘口缺失。
- 新增开赛时段筛选：18:00 前、18:00-23:00、23:00 后/凌晨。
- 切换到不同批次时，如果旧筛选项在新批次中不存在，会自动回退到“全部”，避免误筛空结果。
- 桌面和窄屏布局已检查，无横向溢出。

仍未完成：

- 批量赛事池暂未按国家过滤；需要后端在批量条目中补充标准国家字段。
- 批次之间还没有横向对比看板。
- 批量模拟资金仍未按开赛时间顺序动态扣减并写入完整账本。

## 18. 2026-06-03 实操记录：指定 5 场 fixture_id 批量预测

用户指定比赛：

- `1539000`
- `1489730`
- `1489731`
- `1489732`
- `1489733`

执行结果：

- 首次批量请求暴露后端 bug：`fixtureIds` 为数组时 `_parse_fixture_ids` 会触发 `cannot use 'list' as a set element`。
- 已修复 `_parse_fixture_ids`：支持数组、字符串、空值、重复 ID 和非法片段。
- 已补测试：数组输入 `[1539000, "1489730", 1539000, "bad"]` 应解析为 `[1539000, 1489730]`。
- 修复后再次批量执行，生成本地批次 `#1`。
- 批次 `#1` 成功 0 场、失败 5 场，失败原因均为 API-Football 认证/账户错误。
- 当前 `.env` 已配置 `3319...` 32 位 key，但官方 `/status` 和 `/fixtures?id=1539000` 均返回 `errors.token: Missing application key`。

可用本地历史结果：

- `1539000` 已有本地快照 `snapshot_id=63` 和预测记录 `run_id=113`。
- 该场为 `加拿大 vs Bosnia & Herzegovina`，国际足联世界杯，2026-06-13 03:00 北京时间，BMO Field。
- 模型 pbase：加拿大胜 39.98%，平局 24.90%，Bosnia & Herzegovina 胜 35.11%。
- 预期进球：加拿大 1.49，Bosnia & Herzegovina 1.38。
- 高概率比分：1-1 11.67%，2-1 8.68%，1-0 8.47%，1-2 8.05%。
- 该历史快照未取得胜平负、大小球、让球的优先庄家完整盘口，数据质量 50.5% 低，因此模拟舱不得产生买入信号。

待处理：

- 需要甲方提供可被 API-Sports 官方 `v3.football.api-sports.io` 识别的 API-Football key，或确认当前 key 是否来自 RapidAPI。
- 如果使用 RapidAPI，需要增加 RapidAPI endpoint 与 header 配置分支。
- key 恢复后应重新运行这 5 场，并覆盖批次 `#1` 的失败状态或新建正式批次。

补充执行：

- 用户提供新 key `e377...`，已验证为 API-Sports 官方 Pro 订阅，日额度 7500，有效期至 2026-06-21。
- 已更新本地 `.env`。
- 使用新 key 重跑同一组 5 场，生成本地批次 `#2`。
- 批次 `#2`：成功 3 场、失败 2 场、信号 0 个；实际 HTTP 137 次，缓存命中 11 次。
- API 状态复查：当前已用 138 / 7500。

批次 `#2` 成功场次：

- `1539000`：加拿大 vs 波斯尼亚和黑塞哥维那，国际足联世界杯，2026-06-13 03:00 北京时间。模型概率：加拿大胜 39.98%，平局 24.90%，客胜 35.11%；预期进球 1.49-1.38；盘口缺失，模拟舱不下注。
- `1489730`：Kladno vs Motorlet Praha，3. liga - CFL A，2026-06-06 23:00 北京时间。模型概率：主胜 66.13%，平局 18.37%，客胜 15.49%；预期进球 2.35-1.05；盘口缺失，模拟舱不下注。
- `1489731`：Táborsko II vs Sokol Hostouň，3. liga - CFL A，2026-06-05 23:00 北京时间。模型概率：主胜 33.63%，平局 24.68%，客胜 41.69%；预期进球 1.36-1.55；盘口缺失，模拟舱不下注。

批次 `#2` 失败场次：

- `1489732`：Admira Praha vs Táborsko II，2026-02-28 21:30 北京时间，非赛前比赛，已拦截。
- `1489733`：Jiskra Domažlice vs Králův Dvůr，2026-02-28 21:00 北京时间，非赛前比赛，已拦截。

模型结论：

- 本批次没有任何可执行买入信号。主要原因不是胜率方向，而是 3 场成功预测均没有取得优先庄家的完整胜平负/大小球/让球盘口。
- 低级别联赛赔率覆盖不足是当前真实实操中的重要问题，后续应优先增加“无盘口比赛自动降级为仅模型观察”的批量报告标记。
- 已补中文名录：`Bosnia & Herzegovina -> 波斯尼亚和黑塞哥维那`。

## 19. 2026-06-03 执行记录：本地网页后台常驻启动修复

用户反馈：

- 本地网站经常出现“无法访问此站点”。

排查结论：

- 端口 `8765` 当时没有检测到正在监听的网页服务。
- 旧启动方式本质上是前台进程，终端窗口关闭或进程退出后，网页自然无法继续访问。
- 曾尝试使用 macOS LaunchAgent 常驻服务，但在当前机器上出现 `exit 78` 且无有效日志，稳定性不如直接后台会话。

已完成：

- `start_worldcup_predictor.command` 改为优先使用 detached `screen` 后台会话启动，服务名为 `worldcup_predictor_web`。
- 启动脚本会检查 `/healthz`，成功后自动打开 `http://127.0.0.1:8765`。
- 启动脚本会写入 `storage/web_server.pid`，并把日志输出到 `storage/web_server.log` 与 `storage/web_server.err.log`。
- 新增 `stop_worldcup_predictor.command`，可停止后台 `screen` 会话、旧 LaunchAgent 和残留端口进程。
- `web_server.py` 使用 `ReusableThreadingHTTPServer`，允许地址快速复用，减少刚重启时端口占用导致的失败。
- `.gitignore` 已忽略本地运行日志、PID、SQLite、截图和 API 缓存。

验收结果：

- 双击启动脚本对应的后台启动流程已在本机验证。
- 关闭启动命令后，`screen -ls` 可看到 `worldcup_predictor_web` 后台会话。
- `http://127.0.0.1:8765/healthz` 返回正常。
- `http://127.0.0.1:8765/api/api-status` 返回 API-Football 可用，当前 key 为官方 Pro 订阅。
- 停止脚本可关闭后台服务，随后再次启动可恢复访问。

交付注意：

- 日常使用只需要双击 `start_worldcup_predictor.command`。
- 不再建议甲方直接运行 `python3 -m worldcup_predictor.web_server`，除非用于开发排障。
- 若网页打不开，先重新双击启动脚本；仍失败再查看 `storage/web_server.err.log`。

## 20. 2026-06-03 执行记录：EV 计算路径与模拟舱增强

用户要求：

- 完善 EV 计算以及模拟舱。

本轮原则：

- 不把研究试算 EV 升级为正式投注信号。
- 不改变 `pfinal` 未验收、正式 EV 关闭的治理边界。
- 优先解决“EV 数字不可解释”“亚洲盘口半赢半输不透明”“多市场资金占用可能失真”的问题。

已完成：

- `BetRecommendation` 新增 `ev_calculation` 审计字段。
- 胜平负 EV 展示完整路径：`EV = pbase × odds - 1`，并展开为 `盈利概率 × 净赔付 - 亏损概率`。
- 胜平负同步输出盈亏平衡赔率、盘口隐含概率、模型优势、基础 EV、保守 EV 与闸门通过状态。
- 大小球和让球不再只展示一个 EV 数字，新增比分矩阵结算拆解：全赢、半赢、走水、半输、全输。
- 亚洲盘 EV 统一为：`盈利注权重 × (赔率 - 1) - 亏损注权重`，并输出盈亏平衡赔率。
- 模拟舱资金对象新增动态注额计划字段：启动资金、五等分份数、利润再投入比例、可参与注额、建议单注、单场暴露上限。
- 同一场多个市场同时成为候选时，新增单场总暴露上限，默认不超过当前资金 40%；超出时等比例压缩每个方向注额。
- 质量闸门、正式 EV 闸门、近期样本不足闸门统一使用同一个资金重算函数，避免降级观望后残留旧占用资金。
- 网页模拟舱每个市场新增 EV 计算路径、亚洲盘结算拆分和闸门状态展示。
- 中文 Excel/PDF 报告的模拟舱表新增“EV计算路径”列。
- 公式文档已更新为动态五等分资金策略、庄家优先级和亚洲盘盈亏权重口径。

验收：

- 新增测试覆盖：胜平负 EV 路径、大小球 2.25 盘口半输拆分、单场多市场资金暴露上限。
- 局部测试已通过：`tests.test_predictor`、`tests.test_report`、`tests.test_model_governance`、`tests.test_data_quality`。

仍未开放：

- API 模式正式模拟信号仍关闭。
- `pshr/pfinal` 未达到时间切分样本门槛前，模拟舱继续以“研究复核/观望”为主。
- 大小球和让球仍需独立比分分布层回测验收，不能只因为研究 EV 为正就形成正式买入。

## 21. 2026-06-04 执行记录：官方批次、赛后复盘与模拟舱结算闭环

用户要求：

- 执行下一阶段第二和第三项，目标是最终可交付版本。
- 当前阶段重点仍放在数据、模型计算和模拟舱下注闭环，不提前开放未验证正式 EV。

已完成：

- `batch_runs` 新增 `official_date` 与 `is_official` 字段，可把某个批量结果设为每日官方复盘批次。
- 同一日期、同一范围只保留一个官方批次，避免多个批次同时代表“当天正式预测”。
- 新增 `/api/mark-official-batch`，批量赛事池前端可一键设为官方批次。
- 新增 `/api/daily-review`，按日期生成赛后复盘；优先使用官方批次，若没有官方批次则回退到该日期每场最新预测。
- 新增 `/api/daily-review-report`，导出中文 Excel 复盘。
- 前端新增“赛后复盘”页签，可直接查看复盘摘要、已结算比赛、EV 候选真实结果和待结算比赛，不需要逐个下载 PDF 才能看懂历史结果。
- `paper_bankroll_ledger` 新增结算字段，`/api/sync-results` 同步赛果后会自动结算开放中的模拟舱账本。
- `storage_health` 新增模拟舱开放/已结算条数和官方批次数。
- 交付说明、验收清单和 README 已同步更新到 2026-06-04 口径。

2026-06-03 复盘结果：

- 复盘来源：按日期最新预测。
- 共 `24` 场预测，其中 `15` 场已结算、`9` 场待结算。
- 最高概率方向命中 `9/15`，命中率 `60.0%`。
- 展示融合概率平均 Brier `0.426`，LogLoss `0.759`。
- EV 候选共 `45` 条，已结算 `30` 条，等额盈亏 `-1.98`。
- 高 EV 候选实际亏损 `13` 条，说明当前高 EV 仍存在明显误报，正式 EV 继续关闭是正确决策。
- 正式资金占用 `0` 元，模拟舱没有写入虚假 BUY 记录。

验收：

- 自动化测试：`73 tests OK`。
- 后端 Python 语法检查：通过。
- 前端 JavaScript 语法检查：通过。
- 本地接口验证：`/api/health`、`/api/daily-review`、`/api/daily-review-report` 正常。
- 浏览器验证：本地页面可打开，无前端错误；赛后复盘页可显示已结算、EV 审计和等待结算三块内容。

当前交付判断：

- 产品工作台、批量分析、历史恢复、中文报告、赛后复盘和本地启动已达到可交付研究版。
- 模型正式下注能力尚未达到“可承诺正期望”交付标准；必须继续积累样本、校准 `pfinal`，并单独验收大小球和让球。

## 22. 2026-06-04 执行记录：EV 身份治理与模拟舱状态机 P0 落地

用户要求：

- 基于深度研究报告开始执行改造，优先解决 EV 与模拟舱误导问题。

已完成：

- `BetRecommendation` 新增统一字段：`signal_status`、`ev_layer`、`probability_used`、`ev_pbase_research`、`ev_qmkt_anchor`、`ev_pshr_candidate`、`ev_pfinal_exec`。
- 正式 EV 继续关闭，所有 API 模式研究候选经正式闸门后降级为 `RESEARCH_WATCH`，`ev_pfinal_exec=None`。
- 模拟舱状态机初步统一为：`MODEL_CANDIDATE`、`RESEARCH_WATCH`、`PAPER_BUY`、`NO_MARKET`、`SUSPENDED`。
- 模型分歧、数据质量不足、近期样本不足会进入 `SUSPENDED`，并保留研究 EV 到审计字段，不占用资金。
- `paper_bankroll_ledger` 只写入明确的 `PAPER_BUY`，研究候选不再进入模拟资金流水。
- 资金计划新增现金、占用、可参与注额、单日上限、单市场上限、单联赛上限和高赔上限等基础字段，为后续资金曲线做准备。
- 网页推荐卡片改为分开展示“研究EV(pbase)”与“正式EV(pfinal)”，正式 EV 未开放时直接显示“未开放”。
- 历史记录、批量列表和赛后复盘改为优先显示新状态，旧记录保持兼容。

验收：

- 自动化测试：`73 tests OK`。
- 后端语法检查：通过。
- 本地服务已重启，`/api/health` 正常。
- 本地预测接口确认返回 `signal_status=RESEARCH_WATCH`、`ev_layer=pbase_research`、`ev_pbase_research` 有值、`ev_pfinal_exec=None`。
- 浏览器历史页确认能显示中文状态“研究观察”，无前端错误。

未完成：

- `PAPER_BUY` 暂未开放到正式 API 预测链路，后续必须等 `pfinal` 验收或单独开启纸上演练模式。
- 大小球、让球仍缺少独立分布层回测阈值，不能仅凭 pbase 研究 EV 进入执行。
- 资金策略还没有完整按批次顺序扣减、结算后滚动更新和回撤降档，需要下一阶段继续完善。

## 23. 2026-06-04 执行记录：资金事件时间线与高 EV 异常审计

用户要求：

- 继续下一阶段，围绕数据、模型计算和模拟舱下注闭环推进，不提前开放正式 BUY。

已完成：

- 新增 `worldcup_predictor/paper_bankroll.py`，从 `paper_bankroll_ledger` 生成事件驱动资金时间线。
- 每笔纸上买入拆成两个事件：`RESERVE` 预留注额、`SETTLE` 赛果结算。
- 资金时间线输出现金、预留注额、已实现盈亏、权益、峰值权益、回撤、连亏次数和风险模式。
- 风险模式初步支持：正常、连亏降档、连亏强降档、回撤暂停、人工复核。
- `build_daily_review` 新增 `bankrollTimeline`，赛后复盘页可直接展示模拟舱资金事件。
- `EV候选` 新增赔率区间、模型市场分歧、数据质量、联赛和异常类型字段。
- 新增 `highEvAnomalies` 与 `evAnomalyGroups`，可按市场与赔率区间查看高 EV 异常集中点。
- 中文复盘 Excel 新增“高EV异常”“EV异常分组”“资金事件”工作表。
- 网页“赛后复盘”新增“模拟舱资金事件”和“高 EV 异常分组”模块。

验收：

- 自动化测试：`73 tests OK`。
- 前端语法检查：通过。
- 本地服务已重启，`/api/health` 正常。
- `/api/daily-review?date=2026-06-03` 已返回 `bankrollTimeline`、`highEvAnomalies`、`evAnomalyGroups`。
- 浏览器复盘页确认可显示资金事件面板、高 EV 异常分组和异常明细，无前端错误。

当前观察：

- 2026-06-03 复盘中识别出 `25` 条高 EV 异常，用于风险审计，不代表买入机会。
- 当前资金事件为 `0` 条，因为正式纸上买入仍未开放，账本保持空是正确状态。

仍未开放：

- `PAPER_BUY` 在 API 正式预测链路仍未开放。
- 资金事件时间线已具备，但需要在 `pfinal` 验收后才会自然产生真实纸上买入流水。
- 下一步应继续做大小球/让球专项回测和比分分布层校准，而不是调高 pbase EV 信号。

## 24. 2026-06-05 执行记录：大小球 / 让球盘口专项回测

用户要求：

- 按下一阶段开始执行，重点围绕数据、模型计算和模拟舱下注闭环。

本轮原则：

- 不开放正式 `PAPER_BUY`。
- 不修改核心预测公式。
- 先把大小球与让球的 EV 失真按市场类型、盘口线和赔率区间拆开审计。

已完成：

- `build_daily_review` 新增 `marketLineBacktest`。
- 专项回测仅使用已结算研究 EV 候选和 90 分钟赛果，不影响赛前预测链。
- 输出三层分组：
  - `marketGroups`：按大小球 / 让球整体统计；
  - `lineGroups`：按市场 + 盘口线统计；
  - `selectionGroups`：按市场 + 盘口线 + 方向统计。
- 每个分组输出样本数、盈利/走水/亏损数量、盈利率、亏损率、平均模型概率、平均市场概率、平均研究 EV、平均保守 EV、平均分歧、数据质量、等额净值、高 EV 亏损数和诊断结论。
- 诊断状态包括：
  - `SAMPLE_TOO_SMALL`：样本不足；
  - `UNSTABLE`：亏损集中；
  - `HIGH_EV_RISK`：高 EV 风险；
  - `OBSERVE_POSITIVE`：观察为正；
  - `RESEARCH_WATCH`：继续观察。
- 中文复盘 Excel 新增“盘口专项市场”和“盘口专项线别”工作表。
- 网页“赛后复盘”新增“大小球 / 让球线别回测”板块。

2026-06-03 本地复盘观察：

- 大小球已结算研究候选 `10` 条，等额净值 `-2.36`，状态为“亏损集中”。
- 让球已结算研究候选 `10` 条，等额净值约 `+0.38`，但高 EV 实际亏损 `3` 条，状态为“高EV风险”。
- 线别样本仍普遍不足，不能据此开放执行，只能作为下一步比分分布校准的风险定位。

验收：

- 自动化测试：`74 tests OK`。
- 前端语法检查：通过。
- 本地真实复盘 `2026-06-03` 可生成盘口专项市场和线别统计。

下一步建议：

- 建立比分分布层候选表，保存每场 pbase score matrix 与未来 market-anchor score matrix。
- 对大小球按 `2.25 / 2.5 / 2.75 / 3.0` 等线别累计样本，观察哪些线别长期失真。
- 对让球按 `0 / ±0.25 / ±0.5 / ±0.75 / ±1` 等线别累计样本。
- 在样本不足前，大小球和让球继续只做研究观察，不进入模拟舱执行。

补充修正：

- 发现部分历史样本中 `selection` 含有“球队名 + 盘口”，而 `line` 字段可能是主队基准盘，导致让球方向展示和线别分组存在正负号混淆风险。
- 已新增盘口线归一化逻辑：
  - 大小球 / 让球线别优先从实际 `selection` 提取盘口；
  - `+1.5` 等正向让球保留加号；
  - `-1/-1.5`、`2/2.5` 等分段盘口按均值归一为 `-1.25`、`2.25`。
- `EV候选` 和 `高EV异常` 新增 `selectionDisplay`，网页和 Excel 可优先显示整理后的方向，原始 `selection` 继续保留用于排错。
- 2026-06-03 页面复验：盘口专项显示 `20 条已结算候选`，让球线可见 `+1.5 / +2.25 / +0.5 / -1` 等方向，页面无前端错误。
- 自动化测试更新为 `75 tests OK`。

## 25. 2026-06-05 执行记录：比分分布层校准审计

用户要求：

- 继续下一阶段，围绕数据、模型计算和模拟舱下注闭环推进。

本轮原则：

- 不开放正式 `PAPER_BUY`。
- 不调整核心预测公式或 EV 门槛。
- 先把大小球 / 让球 EV 失真追溯到比分矩阵层。

已完成：

- `build_daily_review` 新增 `scoreDistributionBacktest`。
- 使用预测时保存的 `expectedGoals` 重建同版本 pbase 泊松比分矩阵。
- 新增比赛级比分分布审计：
  - 预期总进球；
  - 实际总进球；
  - 总进球偏差；
  - 实际比分在矩阵中的概率和排名；
  - Top3 比分；
  - 低总进球概率、高总进球概率、尾部概率；
  - 诊断状态：正常观察、低概率比分、单队进球偏差、总进球偏差观察、总进球严重偏差。
- 新增盘口级比分矩阵归因：
  - 大小球 / 让球实际选择方向；
  - 同一比分矩阵下的正收益概率；
  - 盈利注权重、亏损注权重、全赢/半赢/走水/半输/全输质量；
  - 盈亏平衡赔率；
  - 矩阵 EV、推荐 EV 和复算差异；
  - 实际等额盈亏；
  - 高 EV 实际亏损等诊断。
- 中文复盘 Excel 新增“比分分布比赛”和“比分矩阵盘口”工作表。
- 网页“赛后复盘”新增“比分矩阵校准审计”板块。

2026-06-03 本地复盘观察：

- 已结算比分分布样本 `15` 场。
- 盘口矩阵归因样本 `20` 条。
- Top6 比分命中率 `40.0%`。
- 平均预期总进球约 `3.35`，实际总进球约 `2.93`，平均总进球偏差约 `-0.42`。
- 平均绝对总进球偏差约 `1.81`，偏差较大。
- 识别 `6` 条比分矩阵层面的高 EV 实际亏损。
- 复盘样本显示当前大小球高 EV 失真更像是“预期总进球偏高 / 高比分质量偏厚”，而不是单纯 EV 公式错误。

验收：

- 自动化测试：`75 tests OK`。
- 前端语法检查：通过。
- `/api/daily-review?date=2026-06-03` 已返回 `scoreDistributionBacktest`。
- `/api/daily-review-report?date=2026-06-03` 可生成 Excel，大小约 `45KB`。
- 浏览器复盘页确认可显示“比分矩阵校准审计”、Top6 命中、平均总进球偏差和盘口归因，无页面错误。

下一步建议：

- 在 `pbase` 上增加总进球校准因子，先作为候选 `pbase_total_shrink` 审计，不直接进入正式 EV。
- 按赛事类型拆分：国际友谊赛、甲级联赛、杯赛/青年赛分别看总进球偏差，避免用一个缩放因子处理所有比赛。
- 对大小球候选优先做线别校准：`2.25 / 2.5 / 2.75 / 3.0` 分别累计偏差。
- 在样本量达到门槛前，大小球和让球继续保持研究观察，模拟舱不占用资金。

## 26. 2026-06-05 执行记录：模型校准异常复查

用户要求：

- 模型出现严重偏差，检查校准。

本轮原则：

- 不盲目修改算法公式。
- 不开放正式 EV。
- 不开放 `PAPER_BUY`。
- 先确认偏差来源，再决定是否进入模型改造。

校准状态：

- 正式状态：`INSUFFICIENT_DATA`，校准样本不足。
- 合格赛前样本：`15 / 100`。
- 独立比赛：`15 / 100`。
- 校准区间：`3 / 20`。
- 验证区间：`3 / 20`。
- `pfinal`：未批准。
- 正式 EV：关闭。
- `pshr` 当前未优于市场基准，不能作为正式概率。

核心发现：

- 胜平负层：`pbase` 平均平局概率约 `17.9%`，实际平局率约 `26.7%`，存在平局偏低迹象。
- 比分矩阵层：按每场最新自动预测统计，`20` 场已结算样本的平均预期总进球约 `3.41`，实际约 `2.40`，平均误差约 `-1.01`。
- 预期总进球比实际高 `1` 球以上的比赛 `12 / 20`，高 `2` 球以上的比赛 `9 / 20`。
- 大小球高 EV 异常的主要来源不是单纯 EV 公式，而是比分矩阵高总进球区间质量偏厚。
- 发现联赛中文映射存在国家污染：例如非英格兰国家的 `Premier League` 可能被展示为“英格兰足球超级联赛”，非法国国家的 `Ligue 1` 可能被展示为“法国足球甲级联赛”。

已产出：

- 新增校准审计文档：`docs/calibration_audit_2026-06-05.md`。

下一步建议：

- 优先修复赛事名称国家污染，避免复盘和报告误导。
- 新增 `pbase_total_shrink` 作为审计候选比分矩阵，只对比、不执行。
- 用原始 pbase 与候选矩阵分别复算大小球 / 让球，确认 EV 是否收敛。
- 在样本不足前，大小球 / 让球保持研究观察，不进入模拟舱占资。

## 27. 2026-06-05 执行记录：模型胜率与 EV 口径锁定

用户要求：

- 模型胜率出现严重偏差，当前主要任务是确定模型胜率与 EV，确保口径没问题。

本轮判断：

- 胜平负可以称为“模型胜率”，对应 `pbase(selection)`。
- 大小球 / 让球不能直接称为普通胜率，因为亚洲盘存在全赢、半赢、走水、半输、全输。
- 大小球 / 让球展示口径改为“正收益概率”，EV 实际计算仍使用“盈利注权重”和“亏损注权重”。
- 本轮不修改核心预测公式，不调整 pbase 概率，不开放正式 EV。

已完成：

- `BetRecommendation` 新增 `model_probability_label` 和 `ev_probability_basis`。
- 胜平负标记为“模型胜率 / pbase_result_probability”。
- 大小球与让球标记为“正收益概率 / asian_settlement_weight”。
- EV 路径明确输出：
  - 正收益概率；
  - 盈利注权重；
  - 亏损注权重；
  - 盈亏平衡赔率。
- 网页模拟舱、批量摘要、复盘 Excel、中文报告同步更新概率口径。
- 新增审计文档：`docs/model_probability_ev_audit_2026-06-05.md`。

验收：

- 自动化测试：`75 tests OK`。
- 前端语法检查：通过。
- Python 模块编译：通过。

当前边界：

- `pfinal` 仍未批准。
- 正式 EV 仍关闭。
- API 模式仍不得产生正式资金占用。
- 后续模型偏差重点继续放在 pbase 校准和比分矩阵总进球层校准。

## 28. 2026-06-06 执行记录：最终落地交付版整理

用户要求：

- 当前需要最终可落地的项目。

本轮交付定位：

- 项目可以作为“本地研究模拟系统 v1.0”落地。
- 不宣称为已验证盈利模型。
- 不开放正式 EV。
- 不产生正式模拟资金占用。

已完成：

- 新增最终落地交付报告：`docs/final_delivery_report_2026-06-06.md`。
- 新增可编辑 Word 版交付报告：`docs/世界杯预测最终落地交付报告_2026-06-06.docx`。
- 新增 PDF 版交付报告：`docs/世界杯预测最终落地交付报告_2026-06-06.pdf`。
- 更新 `README.md`、`交付说明.md`、`docs/delivery_checklist.md` 的日期、数据覆盖和测试状态。
- 命令行模拟舱输出同步概率身份：胜平负显示“模型胜率”，大小球 / 让球显示“正收益概率”。
- 重启本地网页服务，确认 `http://127.0.0.1:8765` 可访问。

当前本地数据覆盖：

- 预测记录：`166`。
- API 快照：`116`。
- 结构化赔率报价：`1928`。
- Pinnacle 报价：`1529`。
- 已保存 90 分钟赛果：`20`。
- 批量分析批次：`7`。
- 模拟舱正式账本记录：`0`，这是正式 EV 未开放时的正确状态。

验收结果：

- 自动化测试：`78 tests OK`。
- Python 模块编译：通过。
- 前端脚本语法检查：通过。
- 本地健康接口：通过。
- API-Football 状态接口：通过，API 已配置且可连接。
- 模型验收接口：`INSUFFICIENT_DATA`，合格赛前样本 `15 / 100`，正式 EV 关闭。
- 浏览器页面检查：标题、工作台和正式 EV 锁定提示正常，无前端错误。
- PDF 报告：`6` 页，文本抽取正常，首页视觉预览通过。

后续正式化方向：

- v1.0：本地研究模拟系统落地使用，每日跑批积累样本。
- v1.1：样本达到门槛后，重新验收 `pshr / pfinal`。
- v1.2：大小球和让球完成比分矩阵校准后，再考虑模拟舱正式资金占用。

## 29. 2026-06-06 执行记录：实盘验证系统准入闸门

用户要求：

- 需要可落地验证过的实盘下注系统。

本轮判断：

- 当前项目不能被真实表述为“已验证盈利的实盘下注系统”。
- 正确落地路径是升级为“实盘验证系统”：持续建库、结算、回测、审查，通过全部闸门后再进入人工审批。
- 在样本和模型未达标前，真实资金必须保持禁用。

已完成：

- 新增 `worldcup_predictor/live_readiness.py`，统一生成实盘准入状态。
- 新增 `/api/live-readiness`。
- 每次预测 payload 新增 `liveReadiness`。
- 模型审计页新增“真实资金闸门”，展示实盘禁用、未通过项和下一步动作。
- 新增 `docs/live_betting_validation_plan.md`。
- 新增 `tests/test_live_readiness.py`。
- 自动化测试更新为 `78 tests OK`。

当前实盘阻塞项：

- 合格赛前已结算样本不足。
- `pfinal` 未批准，正式 EV 未开放。
- 大小球 / 让球缺少独立比分分布层校准。
- 纸上下注已结算样本不足。
- 真实资金开关固定为关闭，未来即使达到待审批条件也不会自动放开。

## 30. 2026-06-06 执行记录：模型胜率、市场胜率与高 EV 展示修正

用户指出：

- 模型胜率和 EV 可能过高。
- 模拟舱缺少市场胜率。

本轮复核运行 `169`，比赛 `1548442`：

- 胜平负：Kosovo U21 胜 `pbase=66.1%`，Pinnacle 去水市场胜率 `qmkt=51.4%`，差 `14.7` 个百分点，贴近 `15.0` 个百分点复核上限。
- 大小球：大 2.5 正收益概率 `78.5%`，市场去水概率 `51.7%`，差 `26.8` 个百分点，属于单市场分歧超限。
- 结论：大小球研究 EV `43.7%` 不应在主界面按普通研究机会展示。

已完成：

- 单市场 `abs(pbase/qscore - qmkt) > 15%` 时，后端将该市场 EV 标记为 `SUSPENDED_MODEL_DIVERGENCE`。
- 主卡片研究 EV 和保守研究 EV 显示为“已暂停”，原始 EV 只保留在审计字段中。
- 模拟舱新增“市场胜率 / 市场概率”和“模型-市场差”展示。
- 新增回归测试：大小球单市场分歧超限时，只暂停该市场 EV，不误伤整场其他市场。
- 自动化测试更新为 `79 tests OK`。

## 31. 2026-06-06 执行记录：p_adj / paper_EV 概率治理重构

用户要求：

- 严格拆分 `pbase`、`qmkt`、`P_display`、`p_adj`、`pfinal`。
- `P_display` 不得参与 EV、优势判断、推荐或资金计算。
- 旧“保守 EV = EV - 固定折扣 × 赔率”废弃，改为市场收缩概率 `p_adj`。
- U21、友谊赛、缺 xG 或技术统计时，大小球 / 让球不得输出方向。
- Kosovo U21 vs Luxembourg U21 这类异常场景必须触发风险折扣和盘口暂停。

已完成：

- 新增 `worldcup_predictor/risk.py`，统一识别 U21 / 友谊赛 / 缺 xG / 缺技术统计 / 近期样本不足，并生成 `lambdaShrinkFactor` 与风险标签。
- `ModelConfig.max_goals` 默认提升至 `10`，预测结果新增矩阵保留概率、尾部质量、原始 λ、收缩后 λ 与 λ 风险标签。
- 自动预测接入 λ 收缩；深度模式下会把风险识别和收缩原因写入数据处理链路。
- EV 层新增 `shrink_k`、`adjusted_probability`、`paper_expected_value_per_unit`、`decision_status`、`ev_qmkt_anchor`。
- 胜平负计算路径改为：`research_EV = pbase × odds - 1`；`p_adj = qmkt + k × (pbase - qmkt)`；`paper_EV = p_adj × odds - 1`。
- 大小球 / 让球继续使用 score-by-score 结算计算 research_EV，同时正式 EV 关闭；高风险赛事或盘口分歧超限时直接暂停。
- 分歧闸门更新为：`15%-20%` 高风险观察，`>=20%` 模型市场冲突；大小球 / 让球 `>=15%` 直接冲突暂停。
- 页面新增固定说明：`pfinal` 未批准、`formal_EV` 未开放、`P_display` 不参与 EV、大小球/让球依赖专项校准。
- 前端推荐卡片新增 `p_adj`、`shrink_k`、`纸上EV(p_adj)`、`决策状态`，去掉“买入”倾向文案。
- 中文报告和 CLI 输出同步“纸上 EV”口径。
- 新增 Kosovo U21 异常案例回归测试。

验收结果：

- Python 编译：通过。
- 前端脚本语法检查：通过。
- 自动化测试：`80 tests OK`。
- 本地网页 `http://127.0.0.1:8765/` 页面加载完成，无前端错误。
- 示例接口确认推荐字段包含 `p_adj`、`shrink_k`、`paper_EV`、`decision_status`，正式 EV 仍为 `None`。

## 32. 2026-06-06 执行记录：最终交付研究验证版收口

用户要求：

- 直接给出最终可交付版本，中间不再询问。

本轮交付定义：

- 可交付为“本地足球预测研究验证系统”：网页、数据抓取、批量建库、中文 Excel/PDF、模型审计、赛后复盘、交付验收器均可运行。
- 不可宣称为“已验证稳定盈利的实盘下注系统”：合格样本、`pfinal`、OU/AH 专项校准和纸上账本仍未达实盘准入。

已完成：

- 新增 `worldcup_predictor/delivery.py`，提供最终交付验收命令。
- 新增 `final_delivery_check.command`，可双击执行交付验收。
- `pyproject.toml` 新增 `worldcup-delivery-check` 脚本入口。
- 新增 `tests/test_delivery.py`，确保交付状态与真实资金状态分离。
- 前端推荐卡片优先使用后端公开动作标签：`模型候选（未执行）`、`研究观察`、`纸上观察`、`暂停复核`、`市场缺失`。
- 中文报告和 CLI 将内部 `BUY` 文案降级为“模型候选（未执行）”，避免误读为真实下注。
- 当前文档将“保守 EV”更新为 `paper_EV / 纸上 EV` 口径。
- README、交付说明、验收清单和最终交付报告补充最终交付验收命令。

验收结果：

- `python3 -m compileall worldcup_predictor`：通过。
- `node -c web/app.js`：通过。
- `python3 -m unittest discover -s tests -p 'test_*.py'`：`81 tests OK`。
- `python3 -m worldcup_predictor.delivery --full --json`：`DELIVERABLE_RESEARCH_SYSTEM`。
- 交付审计文件：`outputs/delivery_audit/delivery_audit_20260606_223610.json`。
- 浏览器验收：`http://127.0.0.1:8765/` 标题为 `世界杯预测 | 赛前分析工作台`，无前端错误；模型审计页可见“真实资金闸门”“实盘禁用”“正式EV关闭”。

当前关键状态：

- 预测记录：`172`。
- API 快照：`119`。
- 结构化赔率报价：`2035`。
- 赛果：`20`。
- 批量批次：`7`。
- 合格赛前已结算样本：`15 / 100`。
- 独立比赛：`15 / 100`。
- `pfinalStatus=not_approved`，`formalEvEnabled=false`，`canUseRealMoney=false`。

## 33. 2026-06-06 执行记录：基础胜率偏离市场的输入层修复

用户指出：

- 模型与市场仍有较大偏差。
- 当前结果越来越偏离，甚至不如最开始版本。

本轮判断：

- 最开始版本看起来更接近市场，是因为展示概率混入了市场权重；后续拆分 `pbase/qmkt/P_display/pfinal` 后，基础模型自身弱点暴露出来。
- API 模式的 `_profile_from_api` 过度依赖近 5-10 场原始 PPG、进球和失球，且 FIFA rank 默认 `80`，导致弱队面对弱对手刷出好近况时，`pbase` 会被错误推高。
- 友谊赛、U21 和国家队场景中，近期样本噪声更大；单纯 λ 收缩只能降低总进球，不能修正胜负方向。

已完成：

- 新增 `worldcup_predictor/team_strength.py`，建立内部球队强度先验，包含 `elo`、`fifa_rank`、进攻评分和防守评分；U21/U20/U23 自动做青年队折扣。
- `_valid_recent_matches` 新增 `opponent_strength_elo` 和 `opponent_strength_source`，近期比赛明细可追溯对手强度来源。
- `_profile_from_api` 改为先按时间衰减和对手强度校正近期样本：
  - 对强队进球/拿分升权；
  - 对弱队刷进球/拿分降权；
  - 被弱队进球惩罚更大。
- 国家队、友谊赛和青年队场景与强度先验融合：
  - 友谊赛近期样本权重 `15%`；
  - U21/U20/U23 权重 `25%`；
  - 快速模式 `35%`；
  - 深度/批量常规模式 `45%`。
- 更新 `docs/model_formulas.md`、`docs/prediction_data_standard_v1.md`、`docs/model_adoption_spec_v1.md` 和 `README.md`，明确 `pbase` 输入层校正公式和治理边界。
- 新增 `tests/test_team_strength.py`，覆盖青年队先验、对手强度字段和直布罗陀 vs 开曼群岛形态的弱队近况误导案例。

验收结果：

- 针对性测试：`5 tests OK`。
- 全量自动化测试：`84 tests OK`。

当前边界：

- 本轮修复的是 `pbase` 输入层，不是 `pfinal` 审批。
- `formal_EV` 继续关闭，真实资金继续禁用。
- 若修复后 `pbase` 与 `qmkt` 仍然大幅冲突，系统仍应暂停 EV 展示并进入模型复核，而不是强行输出模拟信号。

## 34. 2026-06-06 执行记录：EV 层级收紧与 OU/AH 纸上 EV 暂停

用户要求：

- 下一步解决 EV 的问题。

本轮判断：

- 胜平负 EV 公式 `research_EV = pbase × odds - 1` 可以保留为研究审计。
- 大小球和让球虽然已经按比分矩阵逐比分结算，能正确处理全赢、半赢、走水、半输和全输，但其 `paper_EV` 不能沿用胜平负的 `p_adj × odds - 1` 解释。
- 在比分分布层未完成大小球/让球专项校准前，OU/AH 的 `paper_EV`、`p_adj`、`shrink_k` 容易被误读为可用模拟信号，应从主决策层关闭。

已完成：

- `_apply_score_market_shutdown` 改为对大小球和让球统一输出 `research_audit_only`。
- OU/AH 保留 `research_EV` 和逐比分结算权重审计，但清空主决策层的 `paper_expected_value_per_unit`、`adjusted_probability`、`shrink_k` 和 `ev_pshr_candidate`。
- 前端模拟舱对 OU/AH 显示 `paper_EV 未开放`、`p_adj 未开放`、`shrink_k 未开放`，并展示原因：“比分分布层未完成独立校准”。
- 修复整场模型市场冲突的优先级：整场冲突必须覆盖 OU/AH research-only 状态，所有可用市场都进入 `MODEL_MARKET_CONFLICT`。
- 更新 `docs/model_formulas.md`、`docs/model_adoption_spec_v1.md`、`README.md` 和 `交付说明.md`。
- 新增/更新回归测试，确保 OU/AH 不再输出未校准 `paper_EV`。

验收结果：

- `node -c web/app.js`：通过。
- `python3 -m compileall worldcup_predictor`：通过。
- `python3 -m unittest discover -s tests`：`84 tests OK`。
- 本地样例验证：胜平负仍保留 `paper_EV`；大小球/让球 `paper_EV=None`、`p_adj=None`、`shrink_k=None`，`evDecisionLayer=research_audit_only`。

当前边界：

- `research_EV` 是审计值，不是买入依据。
- OU/AH 在比分分布专项回测通过前不开放 `paper_EV`。
- `formal_EV` 仍只允许未来通过验收的 `pfinal` 进入。

## 35. 2026-06-15 执行记录：1X2 高赔率闸门与 PAPER_BUY 资金关闭

用户要求：

- 继续执行下一步，围绕 EV、模拟舱和模型验收继续收紧。

本轮判断：

- 大小球和让球已被降级为 `research_audit_only`，但胜平负仍可能因 `pbase × odds - 1` 在高赔率方向上产生很大的研究 EV。
- 在 `pfinal` 未验收前，`PAPER_BUY` 不应占用模拟资金；否则未开赛比赛会显示“资金增加/期望资金增加”，容易被误解为正式买入。
- 高赔率冷门方向即使模型概率超过基础下限，也需要单独复核，不能只因为赔率高就成为模拟舱方向。

已完成：

- 胜平负新增 `DEFAULT_MAX_1X2_RESEARCH_ODDS = 4.50` 高赔率复核上限。
- 1X2 候选方向若赔率超过上限，只保留 `research_EV` 审计，动作保持 `WATCH`，不进入纸上资金或正式模拟方向。
- `apply_formal_ev_gate` 扩展为同时拦截 `BUY` 与 `PAPER_BUY`：只要 `formal_ev_enabled=false`，全部降级为 `WATCH`，资金占用归零。
- EV 计算审计新增 `max_odds` 闸门，页面和报告可追溯“高赔率复核上限”是否通过。
- 页面和中文报告文案从“纸上模拟已开启资金占用”改为“研究观察模式；PAPER_BUY 与正式模拟资金占用均关闭”。
- 新增回归测试：
  - 高赔率 1X2 方向只进研究观察；
  - `pfinal` 未验收时 `PAPER_BUY` 必须被治理层降级为 `WATCH`，并关闭 `paperSimulationEnabled`。

验收结果：

- 针对性测试：`31 tests OK`。
- 全量自动化测试：`87 tests OK`。
- `python3 -m compileall worldcup_predictor`：通过。
- 当前模型验收状态：`INSUFFICIENT_DATA`，`formalEvEnabled=false`，`pfinalStatus=not_approved`。
- 当前样本：合格赛前样本 `94 / 100`，独立比赛 `94 / 100`，校准区间 `19 / 20`，验证区间 `19 / 20`。
- 当前候选 `pshr` 仍未通过：验证 Brier `0.6596` 未优于 `pbase 0.6455`，Log Loss `1.0672` 未优于 `pbase 1.0567`。

当前边界：

- 胜平负 `research_EV` 仍可用于诊断，但不能直接解释为买入收益。
- `PAPER_BUY` 暂未开放到 API 正式预测链路。
- 资金曲线和模拟仓位只有在未来 `pfinal/formal_EV` 通过验收后才能重新开启。

## 36. 2026-06-16 执行记录：赛前模拟舱与比赛历史账本

用户要求：

- 当前模拟舱效果可接受，需要在页面里直接看到赛前模拟舱和比赛历史账本。

已完成：

- 新增 `/api/paper-ledger` 汇总接口，统一输出赛前待赛研究观察记录、旧版纸上模拟账本、账本摘要和重复入账提示。
- 历史报告页新增“赛前模拟舱”和“比赛历史账本”两个展示区。
- 赛前模拟舱展示未来未开赛比赛的三类市场方向、赔率、模型概率、市场概率、研究 EV、状态和原因。
- 比赛历史账本展示已入账事件、市场、选择、赔率、下注金额、实现盈亏、赛果比分、结算状态和重复入账风险。
- 对旧版重复入账记录增加前端提示，避免把历史纸上测试误读为正式模拟资金。
- 新增回归测试覆盖赛前记录、历史账本和重复入账识别。

验收结果：

- `/api/paper-ledger`：可返回数据，当前显示赛前模拟舱 `5` 场、历史账本 `8` 笔、重复入账 `2` 笔。
- 页面验收：历史报告页可见“赛前模拟舱”和“比赛历史账本”，前端控制台无错误。
- 针对性自动化测试：`32 tests OK`。

当前边界：

- 新赛前分析仍为研究观察模式，不进入正式资金。
- 历史账本中旧版 `PAPER_BUY` 只作为回溯审计展示，不代表当前正式模拟舱已经开放。
- 下一步应清洗重复入账样本，并把正式模拟舱账本与研究账本分表或分层展示。

## 37. 2026-06-16 执行记录：大小球独立研究校准因子

用户要求：

- 给大小球单独建立校准因子，缓解模型明显偏好大球、大小球 EV 容易过热的问题。

已完成：

- 新增 `worldcup_predictor/total_calibration.py`，从合格赛前快照和 90 分钟赛果中抽取大小球推荐样本。
- 大球、 小球分别计算 `positive_factor`、`win_factor`、`loss_factor`，同一场、同一盘口、同一方向只保留最早赛前快照，避免重复分析放大样本。
- 校准因子按样本可信度收缩到 `1.00`，小样本阶段只温和修正，不让少量比赛过度改写模型。
- 大小球 EV 计算链改为：先按比分矩阵得到原始结算权重，再应用对应方向校准因子，最后输出校准后的 `research_EV`。
- 页面 EV 路径新增“OU校准”行，展示方向因子、样本数、原始 EV、校准后 EV、原始正收益概率和校准后正收益概率。
- 文档同步更新：大小球目前为“研究因子已接入、正式专项验收未通过”，`paper_EV / p_adj / shrink_k / formal_EV` 继续关闭。

当前边界：

- 本次只修正大小球 `research_EV`，不开放正式买入，不产生资金占用。
- 让球尚未建立独立校准因子，仍保持比分矩阵研究审计。
- 后续需要继续累计样本，按盘口线别、赔率区间和赛事类型做分层校准，确认大小球因子是否稳定。

## 38. 2026-06-16 执行记录：pshr/pfinal 候选公式修正

用户要求：

- 修正 `pshr/pfinal` 候选公式，避免旧版市场收缩公式导致模型越来越贴近市场、验证集反而劣于 `pbase`。

问题定位：

- 旧版 `pshr` 在 20 场校准区间直接拟合市场收缩权重 `alpha`，当前样本下 `raw alpha = 1.00`，相当于把候选概率完全变成 `qmkt`。
- 验证区间中 `qmkt` 和旧版 `pshr` 的 Brier、Log Loss 均劣于 `pbase`，因此正式 EV 继续关闭是正确的。

已完成：

- `worldcup_predictor/calibration.py` 将 `PSHR_MODEL_VERSION` 更新为 `pshr_outcome_bias_calibrated_pbase_v2`。
- 新版 `pshr` 不再直接使用市场收缩权重，而是在校准区间拟合主胜、平局、客胜的类别偏差因子：
  - `actual_rate_k = count(actual=k) / N_cal`
  - `pred_rate_k = mean(pbase_k)`
  - `target_rate_k = (actual_rate_k * N_cal + pred_rate_k * prior_samples) / (N_cal + prior_samples)`
  - `factor_k = clamp(target_rate_k / pred_rate_k, 0.75, 1.35)`
  - `pshr_k = normalize(pbase_k * factor_k)`
- 当前默认 `prior_samples=20`，避免 20 场校准样本过度拉动概率。
- 原始市场收缩权重保留为 `rawCalibrationMarketWeight` 审计字段；当前 `fittedMarketWeight=0.0`，表示新版 `pshr` 没有把市场概率直接作为执行混合项。
- 文档同步更新 `docs/model_adoption_spec_v1.md`、`docs/model_formulas.md`、`docs/prediction_data_standard_v1.md` 和 `README.md`。
- 新增测试，防止 `pshr` 被误改回纯市场复制。

验收结果：

- 当前接口状态：`ELIGIBLE_FOR_REVIEW`，`statusLabel=待人工审批`。
- `formalEvEnabled=false`，`pfinalStatus=not_approved`，正式 EV 仍关闭。
- 合格赛前样本 `100/100`，独立比赛 `100/100`，校准区间 `20/20`，验证区间 `20/20`。
- 验证区间指标：
  - `pbase`：Brier `0.6841`，Log Loss `1.1061`，Calibration Error `0.2053`。
  - `qmkt`：Brier `0.7187`，Log Loss `1.1554`，Calibration Error `0.1380`。
  - 新版 `pshr`：Brier `0.6620`，Log Loss `1.0676`，Calibration Error `0.1001`。
- 全量自动化测试：`91 tests OK`。

当前边界：

- 这只是 `pshr` 候选概率通过时间切分的第一阶段验收，不能自动等于 `pfinal`。
- `pfinal/formal_EV` 仍需人工审批、大小球/让球专项验收、策略回测和资金规则确认后才可开放。

## 39. 2026-06-16 执行记录：比分分布独立校准完成

用户要求：

- 完成比分分布层独立校准，解决“比分分布层未完成独立校准，paper_EV 暂不开放”的阻塞。

已完成：

- 新增 `worldcup_predictor/score_calibration.py`，统一处理大小球 `OU` 和让球 `AH` 的比分分布专项校准。
- 样本抽取规则：只纳入合格赛前预测、已同步 90 分钟赛果、具备赔率时间和实际庄家的记录；同一场、同一市场、同一盘口、同一方向只保留最早赛前快照。
- 时间切分规则：每个市场按开赛时间排序，前 70% 作为校准集拟合方向因子，后 30% 作为验证集，只评估不调参。
- 校准对象：按方向计算 `positiveFactor`、`winFactor`、`lossFactor`，再修正正收益概率、盈利注权重、亏损注权重和研究 EV。
- `worldcup_predictor/betting.py` 已接入新校准状态：通过的市场允许显示 `paper_EV` 纸上复核候选；未通过的市场继续关闭 `paper_EV / p_adj / shrink_k`。
- `/api/model-validation` 新增 `scoreDistributionValidation`，可同时查看胜平负 `pshr` 和比分分布专项状态。
- 页面和报告的 EV 路径从“OU校准”改为“分布校准”，可显示市场、方向、因子、样本数、原始 EV 和校准后 EV。
- 新增测试覆盖：比分分布样本抽取、OU/AH 时间切分、通过市场才能打开 paper_EV、formal_EV 仍关闭。

当前真实验收结果：

- 比分分布总样本 `208` 条，独立比赛 `100` 场。
- 大小球：`PAPER_READY`，样本 `104` 条，校准 `72`，验证 `32`；验证集正收益 Brier 从 `0.2406` 降至 `0.2372`，EV MAE 从 `0.8071` 降至 `0.8039`。
- 让球：`REJECTED`，样本 `104` 条，校准 `72`，验证 `32`；正收益 Brier 未明显劣化，但 EV MAE 从 `0.8327` 升至 `0.8645`，因此不得开放 paper_EV。

当前边界：

- 大小球只开放 `paper_EV` 纸上复核候选，不等于正式下注信号。
- 让球仍为研究审计，不开放 `paper_EV / p_adj / shrink_k`。
- `formal_EV` 对所有市场继续关闭，必须等待 `pfinal` 人工审批、资金策略和正式模拟舱验收。

验收结果：

- 自动化测试：`93 tests OK`。
- Python 编译检查：通过。
- 前端脚本语法检查：通过。

## 40. 2026-06-16 执行记录：历史回溯模拟舱

用户要求：

- 对已经分析过的比赛进行回溯分析预测，并进行模拟舱模拟。

已完成：

- 新增 `worldcup_predictor/replay.py`，从本地 `prediction_runs` 留档读取历史预测，并关联 `match_results` 的 90 分钟赛果。
- 新增接口 `/api/prediction-replay?run_id=...`。
- 回溯输出两种模式：
  - `original`：原始快照回放，只使用当时保存的预测、盘口、水位、动作和注额。
  - `current`：当前规则重放，用当前闸门重新解释历史方向，不重新抓盘口、不引入赛后信息、不开放正式资金。
- 回溯结果逐市场展示：胜平负、大小球、让球的方向、赔率、模型概率、市场概率、研究 EV、纸上 EV、是否入选、赛果命中情况、单笔盈亏。
- 回溯资金曲线按入选方向生成起始资金、每笔盈亏、期末资金。
- 历史报告页“回溯数据”按钮已优先读取新回溯接口，展示原始快照回放、当前规则重放、资金曲线和方向明细。
- README 已记录新接口。

验收结果：

- 自动化测试：`94 tests OK`。
- Python 编译检查：通过。
- 前端脚本语法检查：通过。
- 本地服务重启成功：`http://127.0.0.1:8765`。
- 接口实测：
  - 最新记录可返回未结算回溯。
  - 已结算记录 `runId=282` 可返回 90 分钟比分 `2-2`、逐市场结算状态和盈亏。

当前边界：

- 当前规则重放不是重新预测，只是用当前闸门解释历史快照。
- 若历史记录当时没有保存盘口或赛果，则只能显示不可结算或待结算。
- `formal_EV` 仍未开放，回溯资金全部属于纸上模拟。

## 41. 2026-06-17 执行记录：EV 闸门解耦与大小球校准止损

用户反馈：

- 胜平负与市场分歧过大时，大小球和让球也被一刀切暂停，误伤比分分布市场。
- 大小球在因子校准后出现系统性偏低，和市场明显不符。

核查结果：

- 旧规则确实在胜平负最大 `pbase / qmkt` 分歧达到冲突线时，将胜平负、大小球、让球三类市场全部暂停。
- 当前 `/api/model-validation` 实时状态已变化：
  - 胜平负模型状态：`REJECTED / 未通过校准验收`，`pfinalStatus=not_approved`，`formalEvEnabled=false`。
  - 比分分布：`NOT_READY / 比分分布纸上EV未通过`。
  - 大小球：`REJECTED / 大小球独立校准未通过`，`paperEvEnabled=false`。
  - 让球：`REJECTED / 让球独立校准未通过`，`paperEvEnabled=false`。
- 代码此前即使某个比分分布市场为 `REJECTED`，仍会把失败校准因子应用到实时大小球/让球 settlement，导致结果被过度压低。

已完成：

- `worldcup_predictor/betting.py`：
  - 胜平负重大分歧现在只暂停胜平负 EV。
  - 大小球/让球仅追加 `MATCH_WINNER_MODEL_MARKET_DIVERGENCE` 跨市场风险标签，不再被胜平负分歧直接清空。
  - 大小球/让球继续按自身盘口分歧、数据质量、比分分布校准状态独立判断。
- `worldcup_predictor/score_calibration.py`：
  - 只有市场状态 `paperEvEnabled=true` 时才应用比分分布校准因子。
  - `REJECTED` 或 `INSUFFICIENT_DATA` 市场不再用失败因子改写实时 settlement，只保留原始 `research_EV` 审计。
- 文档同步：
  - `docs/model_adoption_spec_v1.md`
  - `docs/prediction_data_standard_v1.md`
  - 文档改为“以 `/api/model-validation` 实时状态为准”，不再静态宣称大小球始终通过。

验收结果：

- 相关测试：`tests.test_predictor` 与 `tests.test_calibration` 共 `32 tests OK`。
- 全量测试：`95 tests OK`。
- Python 编译检查：通过。
- 本地服务已重启：`http://127.0.0.1:8765`。

当前边界：

- 本次修正不开放 `formal_EV`，不批准 `pfinal`，不产生真实下注信号。
- 已保存的历史预测快照不会被自动改写；需要重新生成赛前分析，才会使用新的闸门逻辑。
- 大小球当前为未通过状态，因此不会开放 `paper_EV / p_adj / shrink_k`；页面应以研究观察和原始逐比分结算审计为主。

## 42. 2026-06-17 执行记录：历史快照大小球旧校准回显降级

用户反馈：

- 截图中的 `runId=305` 仍显示大小球旧校准后的 `paper_EV / p_adj / shrink_k`，与当前“大小球独立校准未通过”的状态矛盾。

核查结果：

- 该记录是旧预测快照，数据库中保存了当时旧规则生成的大小球校准结果。
- 当前实时验收已经是：
  - 大小球：`REJECTED / 大小球独立校准未通过`
  - 让球：`REJECTED / 让球独立校准未通过`
- 旧快照直接回显会让页面看起来仍在使用失效因子，形成误导。

已完成：

- 新增 `worldcup_predictor/payload_governance.py`：
  - 读取历史预测时不改写数据库原始快照。
  - 当当前比分分布验收未通过时，将历史大小球/让球旧校准降级为审计值。
  - 清空 `paper_EV / p_adj / shrink_k / pfinal`。
  - 恢复展示原始 `research_EV`、原始正收益概率和原始盘口分歧。
  - 追加 `STALE_SCORE_CALIBRATION_DISABLED` 与 `SCORE_DISTRIBUTION_NOT_VALIDATED` 风险标签。
- `web_server.py`：
  - `/api/prediction`、报告导出、近期预测列表统一使用当前显示治理层。
- `replay.py`：
  - 原始快照回放继续保留当时记录。
  - 当前规则重放使用治理后的显示结果，不再显示旧 paper_EV。

验收结果：

- 相关测试：`tests.test_web_storage` 与 `tests.test_calibration` 共 `15 tests OK`。
- 全量测试：`96 tests OK`。
- Python 编译检查：通过。
- 本地服务已重启：`http://127.0.0.1:8765`。
- 接口抽查 `runId=305`：
  - 大小球 `小 2.5`：`paper_EV=None`、`p_adj=None`、`shrink_k=None`，展示原始 `research_EV=8.65%`。
  - 让球 `Congo DR +1.5`：旧校准停用，因本市场模型与市场分歧超过上限，当前规则暂停。

当前边界：

- 原始快照不删除、不覆盖，仍可用于“当时系统如何判断”的审计。
- 当前推荐、报告和回溯默认使用当前治理规则，避免旧校准污染当前判断。

## 43. 2026-06-17 执行记录：历史比赛模拟舱批量回溯

用户要求：

- 继续完成历史比赛模拟舱回溯功能，不能只看单场，需要能形成历史比赛资金回放视角。

已完成：

- `worldcup_predictor/replay.py`：
  - 新增 `build_history_replay_ledger`。
  - 从本地预测留档和 `match_results` 已同步 90 分钟赛果中生成历史回放账本。
  - 输出两套模式：
    - `original`：原始快照回放，按当时保存的模拟舱动作、注额和赔率结算。
    - `current`：当前规则重放，用现行 EV 闸门、比分分布验收和旧校准降级规则重新解释历史预测。
  - 同一比赛多次预测时，批量历史回放默认只保留最新一次预测，避免重复计算同一场比赛造成盈亏虚高。
  - 输出每笔事件、每场回放摘要、市场汇总、累计资金曲线、已结算场次、待结算场次和去重数量。
- `web_server.py`：
  - 新增接口 `/api/history-replay-ledger`。
- 历史报告页面：
  - 顶部“历史资金趋势”改为“模拟舱资金回溯”。
  - 同时显示原始快照资金曲线和当前规则资金曲线。
  - 新增最近已结算比赛回放明细行，可直接看到比分、原始盈亏、当前规则盈亏和入选方向。
  - 点击回放明细行可继续打开单场回溯预览。

验收结果：

- 自动化测试：`97 tests OK`。
- 前端脚本检查：通过。
- Python 编译检查：通过。
- 本地服务已重启：`http://127.0.0.1:8765`。
- 接口实测 `/api/history-replay-ledger`：
  - 已结算去重后样本：`114` 场。
  - 待赛果样本：`145` 场。
  - 重复预测排除：`51` 条。
  - 原始快照回放：入选 `7` 笔，累计盈亏 `+746.00`。
  - 当前规则回放：入选 `8` 笔，累计盈亏 `-74.00`。
- 浏览器验收：
  - 历史页显示“已结算 114 场 · 去重 51 · 当前规则 -¥74.00”。
  - 资金曲线 SVG 正常生成。
  - 最近回放明细行正常显示。
  - 浏览器控制台无错误。

当前边界：

- 历史批量回放只使用本地已经保存的预测和已同步赛果，不会重新抓盘口。
- 原始快照盈利不代表当前规则盈利；当前规则回放才用于评估现在的模拟舱闸门。
- `formal_EV` 仍未开放，历史回放属于纸上模拟和模型审计。

## 44. 2026-06-18 执行记录：模拟舱一级入口与顶部功能栏重排

用户要求：

- 在功能栏最上方加入“模拟舱”。
- 模拟舱包括回溯模拟舱所有内容。
- 调整顶部功能栏结构，解决新增入口后导航被挤压、历史报告被裁切的问题。

已完成：

- `web/index.html`：
  - 顶部导航新增一级入口“模拟舱”。
  - 将“模拟舱资金回溯”“赛前模拟舱”“比赛历史账本”“单场回溯预览”统一迁入 `cabinView`。
  - “历史报告”页面保留近期预测与报告列表，职责更清晰。
- `web/app.js`：
  - 从历史报告点击“回溯数据”时自动切换到“模拟舱”并展示单场回溯预览。
  - 页面初始化仍默认停留在“预测工作台”，不改变原有分析入口。
- `web/styles.css`：
  - 顶部 Header 调整为稳定的品牌区、功能导航区、状态区三段结构。
  - 功能导航改为 7 等分网格，按钮不再使用固定宽度，避免“历史报告”被裁掉。
  - `cabinView` 使用全宽分析区，并重新打开已迁移的历史资金回放图表。

验收结果：

- 前端脚本检查：通过。
- 相关后端存储测试：`tests.test_web_storage` 共 `8 tests OK`。
- 浏览器验收：
  - 1280 宽度下 7 个功能入口完整显示，无横向溢出。
  - 2048 宽屏下 7 个功能入口同一行显示，导航与右侧状态区有安全间距。
  - 点击“模拟舱”后激活 `cabinView`，回溯资金、赛前模拟舱、历史账本和单场回溯容器均可显示。
  - 浏览器控制台无错误。

## 45. 2026-06-18 执行记录：历史赛前模拟模式

用户澄清：

- 需要的不是“回放已经保存过的预测”，而是能对过往比赛重新做赛前模拟舱预测。
- 例如 2022 年世界杯，程序必须站在开赛前视角预测，不能被未来赛果影响。

已完成：

- `worldcup_predictor/api_football.py`：
  - 新增 `team_finished_fixtures_until`，用于按截止日期抓取球队历史比赛候选。
- `worldcup_predictor/auto_predict.py`：
  - `run_auto_prediction` 新增 `historical_as_of` 参数。
  - 历史模式允许已完赛 fixture 进入预测，但默认把数据截止点锁定为开赛前 1 分钟。
  - 近期比赛、历史交锋、盘口赔率均按截止时间过滤；开赛后或缺少可审计更新时间的盘口会被剔除。
  - 历史模式禁用 API 赛季汇总统计，避免使用赛季结束后的汇总数据穿越。
  - 目标比赛 fixture 快照会剥离 `goals`、`score` 与 `teams.*.winner`，避免已完赛比分进入历史模拟证据链。
  - 输出 `historical_mode`、`historical_as_of` 与 `leakage_policy` 审计字段。
- `worldcup_predictor/web_server.py`：
  - `/api/predict` 支持 `mode=historical_asof`。
  - 历史赛前模拟不写入 `api_snapshots` 和 `market_quotes`，只作为普通预测记录保存，避免污染正式 `pfinal` 验收样本。
- `web/index.html`、`web/app.js`、`web/styles.css`：
  - 左侧控制舱新增“历史模拟时间”和“历史赛前模拟”按钮。
  - 必须填写 fixture_id；历史模拟时间留空时自动使用开赛前 1 分钟。
  - 比赛信息中显示“历史模拟截止时间”，用于区分普通赛前预测。
- `tests/test_safety_and_backtest.py`：
  - 新增历史模式测试，验证已完赛比赛可预测，但开赛后的近期战绩、交锋和盘口不会进入模型。

验收结果：

- Python 编译检查：通过。
- 前端脚本检查：通过。
- 关键测试：`tests.test_safety_and_backtest`、`tests.test_web_storage` 共 `33 tests OK`。
- 全量测试：`98 tests OK`。
- 本地服务已重启：`http://127.0.0.1:8765`。
- 页面 HTML 已确认包含“历史赛前模拟”入口。

当前边界：

- 该模式属于历史赛前回测研究，不代表真实赛前留档。
- API-Football 若不能提供 2022 年比赛的赛前盘口更新时间，盘口会被标为缺失，不会强行用赛后盘口。
- 历史模式不进入正式模型验收样本；未来若要大规模 2022 世界杯回测，应独立建立历史赛前赔率库。

## 46. 2026-06-18 执行记录：EV 三层口径、盘口波动库与分市场验收

用户要求完成三件事：

- 统一 EV 口径和页面展示。
- 扩大并清洗赛前样本，尤其是收盘赔率和盘口变化。
- 分别验收胜平负、大小球、让球，不能混用一套校准逻辑。

已完成：

- `worldcup_predictor/web_server.py`：
  - 每条推荐新增标准 `evLayers`，固定为研究 EV、纸上 EV、正式 EV 三层。
  - `/api/model-validation` 新增 `marketValidation`，把胜平负、大小球、让球拆成独立状态、样本数、切分和失败门槛。
  - 新增 `/api/collect-odds-snapshot`，可按 fixture_id 只抓取并保存赔率快照，不运行完整预测。
- `worldcup_predictor/storage.py`：
  - 新增 `odds_snapshots` 表，保存赛前盘口、水位、去水概率、时点和收盘候选标记。
  - 新增 `record_odds_movement_snapshot` 与 `odds_movement_coverage`。
  - `market_dataset_coverage` 和 `storage_health` 增加盘口波动库覆盖统计。
- `worldcup_predictor/odds_monitor.py`：
  - 新增轻量盘口快照采集器，支持单场或多场 fixture_id 抓取 odds 并记录到盘口波动库。
- `web/index.html`、`web/app.js`、`web/styles.css`：
  - 推荐卡片优先读取三层 EV 结构。
  - 模型审计页新增三市场独立验收卡片。
- `tests/test_safety_and_backtest.py`：
  - 新增赛前盘口快照入库测试，确认赛后/缺时点数据不会进入盘口波动库。

验收结果：

- Python 编译检查：通过。
- 前端脚本检查：通过。
- 全量测试：`99 tests OK`。
- 本地服务已重启：`http://127.0.0.1:8765`。
- `/api/health` 已显示 `odds_snapshots` 字段；当前为 0，表示尚未执行真实盘口波动采集。
- `/api/model-validation` 已输出分市场验收：
  - 胜平负：`REJECTED`，108 个合格样本，失败项为 `pshr 未显著劣于市场基准`。
  - 大小球：`REJECTED`，113 个样本，失败项为校准后 Brier 和 EV 误差劣化。
  - 让球：`REJECTED`，113 个样本，失败项为校准后 EV 误差劣化。

当前边界：

- 盘口波动库只记录从现在开始的赛前快照，无法补回本地未抓到的历史每小时水位。
- 轻量盘口采集器不会污染 `api_snapshots`、`market_quotes` 或 pfinal 验收样本。
- 正式 EV 仍保持关闭；当前只完成口径统一、数据底座和分市场验收展示，不代表模型已经具备真实下注准入。

## 47. 2026-06-18 执行记录：开放研究/纸上 EV 可见层

用户要求：

- 当前需要开放 EV，尤其是大小球和让球不再只显示“未开放”，要能看到候选计算结果用于继续观察和复盘。

本轮执行口径：

- 开放的是“研究/纸上 EV 可见层”，不是正式 `formal_EV`，也不是实盘买入。
- 大小球和让球在比分分布专项验收未通过时，仍可显示 `p_adj`、`shrink_k`、`paper_EV` 候选值。
- 该候选值只用于研究观察和回溯复盘，不触发 `PAPER_BUY`，不占用模拟资金，不写成正式收益信号。
- 数据质量风险、盘口极端分歧、U21/友谊赛缺 xG 或技术统计等硬风险仍会保留暂停逻辑。

已完成：

- `worldcup_predictor/betting.py`：
  - 新增 `paper_research_open` 决策层。
  - 大小球/让球在专项验收未通过时，不再清空 `paper_EV / p_adj / shrink_k`，而是保留可见计算。
  - 动作固定为 `WATCH`，`stake=0`，`paperSimulationEnabled=false`，`formalExecutionEnabled=false`。
  - 风险标签新增 `PAPER_EV_RESEARCH_OPEN`，明确这是研究开放，不是资金开放。
- `tests/test_predictor.py`：
  - 更新回归测试，确认大小球/让球可展示纸上 EV 候选，但仍不进入资金。

验收结果：

- Python 编译检查：通过。
- 前端脚本语法检查：通过。
- 全量测试：`99 tests OK`。

当前边界：

- `formal_EV / pfinal` 仍未审批，不开放真实资金或正式模拟资金。
- 页面中 `paper_EV` 可作为候选观察值，但判断是否买入仍必须等待模型验收、赛后回测和资金规则确认。
- 若后续要真正开启模拟舱买入，需要单独做 `pfinal` 审批、分市场回测达标和账本风控验收。

## 48. 2026-06-18 执行记录：修复本地无法访问与数据库启动锁

用户反馈：

- 本地网页无法访问，`http://127.0.0.1:8765` 打不开。

核查结果：

- 端口 `8765` 没有监听，后台服务已停止。
- 重新启动时日志发现 SQLite 在启动后多接口并发访问时触发 `database is locked`。
- 根因是每次建立数据库连接都会执行 `init_db()` 和历史盘口结构回填；页面加载时多个接口同时触发初始化，容易抢锁。

已完成：

- `worldcup_predictor/storage.py`：
  - SQLite 连接增加 `timeout=30.0`。
  - 增加 `PRAGMA busy_timeout = 30000`。
  - 增加进程级初始化锁 `_INIT_LOCK` 和已初始化数据库集合 `_INITIALIZED_DATABASES`。
  - 同一数据库在同一服务进程内只执行一次初始化和回填，减少启动时并发锁冲突。
- `start_worldcup_predictor.command`：
  - 尝试切换常驻服务模式后发现当前机器 LaunchAgent 返回 `EX_CONFIG`，已退回可靠的 `screen` 后台会话模式。

验收结果：

- 服务已重新启动：`http://127.0.0.1:8765`。
- 连续 5 次访问首页和健康接口均成功。
- 数据库相关测试：`34 tests OK`。
- Python 编译检查：通过。

当前边界：

- 当前启动方式仍是后台会话模式，不是系统级常驻服务；重启电脑后仍需双击启动脚本。
- 数据库锁问题已降低风险，但后续若要做真正长期服务，建议单独实现更稳的本地守护进程或桌面打包启动器。

## 49. 2026-06-18 执行记录：模拟舱只展示真实下注账本

用户要求：

- 模拟舱页只显示模拟舱下注内容，不再混入赛前观察。
- 每个方向只下注一次。
- 资金图表横轴为日期，纵轴为资金，起始资金固定为 1000。
- 去掉“赛前模拟舱”，历史账本改为横向结构。

已完成：

- `worldcup_predictor/storage.py`：
  - 新增写入前重复检查，同一 `fixture_id + market + selection + line` 已存在时不再重复写入模拟舱账本。
- `worldcup_predictor/paper_bankroll.py`：
  - `/api/paper-ledger` 改为只返回真实入账的模拟舱下注记录。
  - 保留旧账本兼容，但展示与资金曲线按同一方向去重。
  - 新增从 1000 起步的账本资金曲线，按结算日期累计已实现盈亏。
  - `preMatch` 固定为空，避免把未入账观察信号误认为模拟舱下注。
- `web/index.html` / `web/app.js` / `web/styles.css`：
  - 模拟舱页删除“赛前模拟舱”区块。
  - 资金曲线改为模拟舱账本资金曲线。
  - 增加起始资金、当前资金、已实现盈亏、占用资金、单方向规则、折叠重复摘要。
  - 历史账本改为横向表格，点击行仍可进入单场回溯。
  - 点击“模拟舱”标签时主动刷新账本。

验收结果：

- 当前接口返回：起始资金 1000，真实下注账本 7 笔，旧重复折叠 1 笔，赛前模拟舱 0 条。
- 浏览器验收：模拟舱页无 `preMatchCabin`，横向账本 7 行，页面无横向溢出。
- 前端脚本语法检查：通过。
- 相关测试：`34 tests OK`。
- 全量测试：`99 tests OK`。

当前边界：

- 旧数据库中已经存在的重复历史记录不会物理删除，只在展示、资金曲线和通用资金时间线中折叠。
- 后续若要做正式审计，可单独增加“重复入账清理/归档”工具，将旧重复记录标记为作废而不是删除。

## 50. 2026-06-18 执行记录：模拟舱新增实时持仓

用户要求：

- 模拟舱里除了历史账本，还要有实时模拟舱。
- 实时模拟舱用于展示“经预测买入但尚未开赛”的比赛。

已完成：

- `worldcup_predictor/paper_bankroll.py`：
  - `/api/paper-ledger` 新增 `liveCabin` 与 `summary.liveCabinCount`。
  - 实时模拟舱只从真实模拟下注账本中筛选 `OPEN` 记录。
  - 如果记录已有可解析开赛时间且已经开赛，则不再显示为实时未开赛持仓，避免把待同步结算的旧记录误认为赛前持仓。
- `web/index.html` / `web/app.js` / `web/styles.css`：
  - 模拟舱页新增“实时模拟舱”区块，放在资金曲线与历史账本之间。
  - 实时持仓卡片展示比赛、联赛、开赛时间、盘口方向、赔率、注额、模型/市场概率、研究 EV、入账时间。
  - 点击实时持仓卡片可回到对应单场分析。
  - 空状态明确提示：只有进入 `PAPER_BUY/BUY` 且尚未结算的比赛会出现在这里。

验收结果：

- 新增单测覆盖：未来未开赛的 `PAPER_BUY` 会进入实时模拟舱；已开赛但未结算的 `OPEN` 记录不会进入实时未开赛持仓。
- 前端脚本语法检查：通过。
- 全量测试：`100 tests OK`。
- 本地服务已重启：`http://127.0.0.1:8765`。
- 当前真实账本状态：历史账本 7 笔，均已结算；实时模拟舱 0 笔，属于正常空状态。

当前边界：

- 实时模拟舱不展示 `WATCH` / `RESEARCH_WATCH`，也不展示未写入账本的研究候选。
- 若正式 EV 或纸上买入闸门未开放，新的赛前预测不会自动进入实时模拟舱；必须实际写入 `paper_bankroll_ledger` 后才会显示。

## 51. 2026-06-19 执行记录：页面结构减负与信息压缩

用户要求：

- 每个页面删掉约 30% 低价值文字和内容，让结构更干净。
- 不能影响任何功能、按钮、接口、计算和导出。

已完成：

- `web/index.html`：
  - 压缩左侧参数说明、历史回放说明、默认等待态、数据处理、复盘、模拟舱、历史报告等静态文案。
  - 保留全部输入项、按钮、视图、导出入口和数据容器。
- `web/app.js`：
  - 压缩搜索、批量、同步、EV 路径、复盘、模拟舱、批次池、回溯等动态文案。
  - 保留全部渲染字段、事件绑定、API 调用和计算结果展示。
- `web/styles.css`：
  - 新增信息减负覆盖规则，对长原因、EV 说明、批次备注、复盘小字、流程说明做 1-2 行视觉限高。
  - 收紧比赛 Header、概率卡、比分模块、模拟舱方向、历史账本、实时持仓等区域的间距和高度。

验收重点：

- 本轮只做 UI 与文案压缩，不改模型、EV、模拟舱账本、API 或导出逻辑。
- 目标是让预测工作台、批量赛事池、数据处理、模型审计、赛后复盘、模拟舱、历史报告都更轻、更易扫读。

## 52. 2026-06-19 执行记录：按红框标注完成 UI 二次清理

用户要求：

- 按截图红框逐项处理，继续删减低价值展示内容。
- 默认保留深度模式，不再在页面暴露过多参数。
- 预测页、模拟舱、数据处理、模型审计、批量赛事池、历史报告都要更清爽，但功能不能受影响。

已完成：

- `web/index.html`：
  - 顶部移除“90 分钟”状态标签，仅保留 API 状态。
  - 左侧隐藏抓取深度、资金参数、市场权重、优势门槛和研究模式说明；对应表单字段仍保留为隐藏字段，计算与提交逻辑不受影响。
  - 将“生成赛前分析”和“历史回放”集中到左侧主操作区。
- `web/app.js`：
  - 模拟舱方向卡只展示赔率、模型胜率、市场胜率、模型-市场差和 EV，删除 p_adj、shrink_k、研究 EV、正式 EV、决策状态等冗余格子。
  - EV 说明区只保留通过/未通过闸门标签与必要暂停提示，删除公式长文、复核长文和结算拆分说明。
  - 回溯预览改为单表结构，只展示“当时模拟舱方向”，避免原始快照和当前规则重复堆叠。
  - 批量赛事池历史项删除批次名称、备注编辑和官方批次按钮，仅保留恢复批次。
  - 历史报告单元格改为只展示赔率、模型胜率和 EV，并新增分页。
- `web/styles.css`：
  - 预测工作台概率卡文本居中，删除主胜卡“模型主倾向”角标。
  - 模拟舱、历史报告、数据处理、模型审计、赛后复盘等页面默认隐藏左侧控制舱，主内容自动占满宽度。
  - 数据处理页隐藏流水线、盘口趋势提示和资金趋势图，让状态特征、样本和公式内容更集中。
  - 模型审计页隐藏公式路径、同步按钮和运行提示长文，保留关键质量与验收信息。
  - 模拟舱页资金摘要只保留起始资金、当前资金和已实现盈亏。
  - 历史报告操作列调整宽度并增加分页，降低右侧按钮被裁切的风险。

当前边界：

- 本轮属于展示层清理：隐藏字段仍然参与原有 payload，历史回放、批量分析、导出、模拟舱账本和 API 调用都继续保留。
- 若后续要进一步压缩页面，可优先处理历史报告表格密度和右侧回溯抽屉，但不建议再删核心概率与 EV 信息。
