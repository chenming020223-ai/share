# 甲方确认需求

日期：2026-05-21

## 已确认配置

| 项目 | 甲方选择 | 项目默认行为 |
|---|---|---|
| 数据源 | API-Football | 使用本地 `.env` 中的 `API_FOOTBALL_KEY` |
| 模拟舱规则 | 只有正期望才下注 | 网页端不做强制下注 |
| 预测口径 | 只看 90 分钟赛果 | 报告和页面均标注 90 分钟 |
| 部署方式 | 只在电脑本地运行 | 默认 `127.0.0.1:8765` |
| 项目名 | 世界杯预测 | 页面、报告和 Logo 使用该名称 |
| 主色调 | 橘色 | 页面主按钮、状态、Logo 使用橘色 |
| 报告导出 | 需要中文报告导出 | 每次预测后可导出 Excel 中文报告和 PDF 中文报告 |

## 密钥处理

真实 API key 保存在本机 `.env` 文件中。该文件已被 `.gitignore` 忽略，不进入代码交付或版本管理。

## 当前边界

- 模拟舱只用于纸上研究和回测。
- 系统不连接真实投注平台。
- 正期望来自当前模型估计，不代表确定收益。
- API-Football 真实可用字段取决于赛事 coverage。
